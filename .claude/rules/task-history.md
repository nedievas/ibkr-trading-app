# Task History

All planned tasks are complete through Phase 14. Phase 11 (Auto Technical Analysis) is fully landed — Tasks #52–#55 done; plan at `.claude/plans/auto-analysis.md`. Phase 12 (Setup Suggestions + Position Safety) Tasks A/B/C of the plan landed as Tasks #56–#58; plan at `.claude/plans/setup-suggestions.md`. Phase 13 (Trading-Style Modes + VWAP companion) plans at `.claude/plans/trading-styles.md` and `.claude/plans/vwap.md`; VWAP Tasks A/B landed as Tasks #59–#60, Trading-Styles Tasks A/B/C/D landed as Tasks #61–#64 (Task E = docs, this file). Phase 15 (Order Flow Insights — ChartWindow only) plan at `.claude/plans/order-flow-insights.md`; Task A (Volume Profile / CW-1) landed as Task #74. Tasks B/C (CVD sub-pane, divergence detector) pending. Phase 16 (Notifications — per-event tones + voice + in-app toasts) plan at `.claude/plans/notifications.md`; all five tasks A–E landed as Tasks #75–#77 (with Tasks A/B already shipped, C/D/E shipped together). Phase 17 (Comprehensive State Persistence) plan at `.claude/plans/state-persistence.md`; all 9 tasks landed as Tasks #78–#85. Task 7 confirmed a no-op (ImGui TabItem).

## Phase 1: Infrastructure
- [x] **Task #1** — CMake configuration with dependency validation
- [x] **Task #2** — ImGui base with dockable window system

## Phase 2: Core Features
- [x] **Task #3** — Login window (paper and live accounts) → `RenderLoginWindow()` in `main.cpp`
- [x] **Task #4** — Candlestick chart window with indicators → `src/ui/windows/ChartWindow.{h,cpp}`

## Phase 3: Data Feeds
- [x] **Task #5** — News window (Market / Portfolio / Stock tabs) → `src/ui/windows/NewsWindow.{h,cpp}`
- [x] **Task #6** — Trading window (order book + execution) → `src/ui/windows/TradingWindow.{h,cpp}`

## Phase 4: Advanced Features
- [x] **Task #7** — Market scanner → `src/ui/windows/ScannerWindow.{h,cpp}` + `src/core/models/ScannerData.h`
- [x] **Task #8** — Portfolio dashboard → `src/ui/windows/PortfolioWindow.{h,cpp}` + `src/core/models/PortfolioData.h`

## Phase 5: Multi-Instance & Group Sync
- [x] **Task #9** — Window reopen from menu — all 6 windows togglable via Windows menu (`bool& open()` on every window)
- [x] **Task #10** — Window groups (`src/core/models/WindowGroup.h`) — `GroupState`, `WindowPreset`, `DrawGroupPicker`; `BroadcastGroupSymbol` + `g_groupSyncInProgress` guard in `main.cpp`; presets menu (Trading Focus, Research, Full Desk)
- [x] **Task #11** — Multi-instance windows — up to 4 simultaneous Chart, Order Book, and Scanner windows; `ChartEntry`/`TradingEntry`/`ScannerEntry` structs with per-entry reqId allocation; `SpawnChartWindow`/`SpawnTradingWindow`/`SpawnScannerWindow`; "+ New" buttons in Windows menu
- [x] **Task #12** — Group sync fix — `ApplyTradingSymbol` helper so group broadcast re-subscribes IB market data + depth (not just display); reqId collision audit (TradingDepth moved to 120+, ScannerBase moved to 1000+)
- [x] **Task #13** — Group picker visibility fix — replaced Unicode glyphs with ASCII `G1`/`G-` labels, moved picker to leftmost toolbar position in all windows, raised button alpha
- [x] **Task #14** — Default group assignment — instance N auto-assigned to group N on spawn; NewsWindow defaults to group 1

## Phase 6: UX Polish & Resilience
- [x] **Task #15** — Current price line — dashed grey horizontal line at last close price; right-aligned price tag inside the chart clip rect (`DrawOverlays()` in `ChartWindow.cpp`); uses `pMax.x` (actual pixel boundary) for correct placement
- [x] **Task #16** — Disconnection badge — `LostConnection` added to `ConnectionState` enum; on unexpected disconnect keeps trading UI alive (nulls client, doesn't destroy windows); orange `DISCONNECTED` badge with background rect rendered left of `[LIVE]`/`[PAPER]` in menu bar
- [x] **Task #17** — Auto-reconnect — `StartSilentReconnect()` in `main.cpp`; `g_reconnectNextAttempt` timer (5s interval) polled each frame when `LostConnection && !g_IBClient`; on reconnect success re-subscribes each chart/trading window with its current symbol+timeframe instead of recreating windows; `getTimeframe()` added to `ChartWindow`, `getSymbol()` added to `TradingWindow`

## Phase 7: Testing Infrastructure
- [x] **Task #18** — Extract `ParseStatus` / `ParseIBTime` to `src/core/services/IBKRUtils.h` as inline free functions; move `IBKRClient::Push()` from `private` to `protected`; remove private static declarations from `IBKRClient.h`
- [x] **Task #19** — `tests/CMakeLists.txt` — Catch2 v3.7.1 via FetchContent; `tests-core` (no IB API dep) and `tests-ibkr` (links ibapi-lib) targets; `IBKR_SANITIZE` option applies ASan+UBSan to `tests-core`
- [x] **Task #20** — `tests/test_market_data.cpp` — 20+ cases: all Timeframe label/seconds/barsize/duration helpers; `IsUSDST` DST boundary cases (March/November 2024); `BarSession` session classification at all transition boundaries (EDT and EST)
- [x] **Task #21** — `tests/test_models.cpp` — enum string helpers (`OrderSideStr`, `OrderTypeStr`, `TIFStr`, `OrderStatusStr`, `ScanPresetLabel`, `AssetClassLabel`); struct default-value assertions for `Order`, `Fill`, `DepthLevel`, `ScanResult`, `ScanFilter`, `Position`, `AccountValues`, `Bar`
- [x] **Task #22** — `tests/test_ibkr_utils.cpp` — `ParseStatus` all IB string variants including case-sensitivity; `ParseIBTime` for 8-digit date (noon UTC mapping), Unix timestamp round-trips, calendar correctness across multiple dates
- [x] **Task #23** — `tests/test_ibkr_queue.cpp` — `TestableIBKRClient` subclass; dispatch tests for all major `IBMessage` variants; null-callback safety; ordering; queue-drain idempotency
- [x] **Task #24** — CI: all three platform jobs (`build-linux`, `build-macos`, `build-windows`) now configure with `-DIBKR_BUILD_TESTS=ON` and run `ctest`; FetchContent dep cache added; new `sanitize-linux` job runs `tests-core` under ASan + UBSan

## Phase 8: UX Redesign & Responsiveness
- [x] **Task #25** — MSVC NOMINMAX fix — `$<$<CXX_COMPILER_ID:MSVC>:NOMINMAX>` added to `target_compile_definitions` in `CMakeLists.txt`; resolves C2589 `min`/`max` macro conflict on Windows CI
- [x] **Task #26** — Windows menu restructure — `Windows → IBKR → ...` submenu hierarchy; `ImGuiItemFlags_AutoClosePopups = false` keeps submenu open while toggling windows; `kMaxMultiWin` raised from 4 to 10 for Chart, Order Book, Scanner, and News
- [x] **Task #27** — Dynamic window title labels — all windows display `"<Type> <Symbol> <Group>###stable_id"` (e.g. "Chart AAPL G1") in both the IBKR submenu and the window title bar; uses `###id` triple-hash for stable ImGui window identity; `ImGuiWindowFlags_NoFocusOnAppearing` prevents newly opened windows from stealing focus and closing the menu
- [x] **Task #28** — News feed multi-instance — `NewsEntry` struct with per-instance reqId ranges (2000–3699); `SpawnNewsWindow(idx)` wires per-instance callbacks; market news broadcasts to all instances; per-instance stock/portfolio/article routing by reqId range; default group = instance index
- [x] **Task #29** — Settings panel — floating `RenderSettingsWindow()` with Small / Medium / Large font size radio buttons; `io.FontGlobalScale` (0.85 / 1.0 / 1.5) drives scaling; `g_baseStyle` saved once after style setup and restored before each `ScaleAllSizes()` call to prevent compounding
- [x] **Task #30** — `src/ui/UiScale.h` — `em(px)` converts design-time pixel values (authored at 13 px base) to font-scale-aware sizes; `FlexRow` struct implements CSS `flex-wrap: wrap` for ImGui toolbars — calls `SameLine()` only when the next item fits on the current line; static helpers `buttonW`, `checkboxW`, `textW` estimate item widths
- [x] **Task #31** — FlexRow applied to all toolbars — `ChartWindow::DrawToolbar()` (group picker, symbol input, quick symbols, timeframe, zoom, intraday toggles, 7 indicator checkboxes), `DrawAnalysisToolbar()`, `TradingWindow` order-book header, `NewsWindow::DrawToolbar()`, `ScannerWindow::DrawToolbar()`; all `SetNextItemWidth(N)` calls converted to `SetNextItemWidth(em(N))`
- [x] **Task #32** — Chart window inner rows responsive — `DrawTradePanel()` (Trade label, Qty, order type, trail, session, TIF, BUY/SELL, armed hint), `DrawInfoBar()` (symbol/date, O/H/L/C, change, VWAP, PnL), `DrawPositionStrip()` (Position, Entry, Last, Unreal P&L, Comm, Net) all use `FlexRow`; `DrawPositionStrip` child window height is now dynamic (2 × lineHeight) to accommodate wrapped content
- [x] **Task #33** — TradingWindow resizable panels — `m_topHeightRatio` (default 0.65) and `m_bookWidthRatio` (default 0.54) stored as member variables; draggable 4px splitter bars between left/right top panels (`ImGuiMouseCursor_ResizeEW`) and top/bottom panels (`ImGuiMouseCursor_ResizeNS`); splitters highlight on hover/drag; NBBO bar uses `FlexRow`; Order Entry symbol row uses `FlexRow`; form label column uses `SameLine(em(75))` to scale with font; BUY/SELL and submit buttons scale with `em()`

## Phase 9: Order Type Expansion
- [x] **Task #34** — `src/core/models/OrderData.h` — `OrderType` enum expanded to 13 values (Market, Limit, Stop, StopLimit, Trail, TrailLimit, MOC, LOC, MTL, MIT, LIT, Midprice, Relative); `Order` struct gains 5 fields (`auxPrice`, `trailingPercent`, `trailStopPrice`, `lmtPriceOffset`, `outsideRth`); `OrderTypeStr()` extended to cover all 13 types; `tests/test_models.cpp` updated with new enum string assertions and struct default checks
- [x] **Task #35** — `IBKRClient::PlaceOrder` redesigned to accept `const core::Order&` (replaces 9-param string API); `TradingWindow::OnOrderSubmit` callback simplified to `std::function<void(const core::Order&)>`; `main.cpp` `SpawnTradingWindow` lambda and both `SpawnChartWindow` lambdas (`OnOrderSubmit`, `OnModifyOrder`) updated to use struct-based API; all 13 order types mapped to correct IB `::Order` fields in `PlaceOrder` switch
- [x] **Task #36** — `TradingWindow` order entry expanded to 13 types; per-type conditional field rendering (trail $/% toggle + stop cap, trigger price, lmt offset, peg offset, price cap); `ValidateOrder()` per-type switch; `SubmitOrder()` populates full `core::Order` via explicit `kTypeMap[]`; `DrawConfirmationPopup()` shows correct price fields per type; `PlaceDomOrder` updated to new callback signature; TIF combo locked to DAY for MOC/LOC; Outside RTH disabled for close orders; submit button label and Open Orders price column show correct values for all 13 types via `priceSummary` helper and per-type switch
- [x] **Task #37** — `ChartWindow` order type expansion — `OnOrderSubmit` callback changed to `std::function<void(const core::Order&)>`; `OrderTypeDef` struct extended with `coreType`, `firstIsAux`, `tifLocked`, `noRth` flags; `kOrderTypes[]` replaced with 13-entry table matching TradingWindow types (removed "Adjustable Stop" duplicate, fixed LIT to dual-price with `firstIsAux=true` trigger→limit two-click, fixed MIT to single trigger-price click); `DrawTradePanel()` gains $/% toggle for Trail, lmt offset input for Trail Limit, peg offset for Relative, TIF lock + session disable for MOC/LOC; `fireOrder` lambda and all three `OnOrderSubmit` call sites (ctrl+click popup, single-price phase 1, dual-price phase 2) rewritten to build `core::Order` directly; LIT pending order overlay draws two legs (TRIG + LMT); `main.cpp` `SpawnChartWindow` `OnOrderSubmit` lambda simplified to copy-and-stamp; `UpdateChartPendingOrders` extended for LIT, LOC, MIT order types
- [x] **Task #38** — Expanded order blotter columns — `TradingWindow::DrawOpenOrders()` expanded from 9 to 14 columns (added Aux price, TIF, separate Filled/Avg$ columns, Comm, Time); `OrdersWindow` Open and History tabs expanded from 10 to 15 columns (added Aux price, TIF, Ext RTH, Comm in open tab, Time, Reject reason column in history); per-type Price/Aux rendering covers all 13 order types; horizontal scroll (`ImGuiTableFlags_ScrollX`) on all three tables; row tint added to `DrawOrderRow`
- [x] **Task #39** — Chart window order confirmation popup — `DrawConfirmPopup()` modal in `ChartWindow.cpp`; "Transmit Instantly" checkbox (default ON) in `DrawTradePanel()`; when OFF every order path (BUY/SELL buttons, single chart click, dual-price second click) stages the order in `m_pendingConfirmOrder` and opens the modal instead of firing; Ctrl+click always forces the modal regardless of the toggle; popup shows all fields editable per type (Limit, Stop, StopLimit, Trail $/%, TrailLimit with lmt-offset + stop-cap, MIT trigger, LIT trigger+limit, Midprice cap, Relative offset+cap, Market/MOC/MTL display-only); TIF, Outside RTH, and estimated value shown; Confirm fires `OnOrderSubmit`, Cancel/Escape disarms the chart-click state; hint text updates to reflect current mode

## Phase 10: API Depth & Cross-App Integration

**ReqId allocations for Phase 10 (no overlaps with existing layout):**
- Watchlist contract details (transient, one per instance in-flight): 6900–6909
- Watchlist market data: 7000–7999 (watchlistIdx × 100 + symbolSlot; 10 instances × 100 symbols)
- Tick-by-tick: 130–139 (one per TradingWindow instance, extends 110–129 mkt/depth block)
- Symbol search: 8000 (single in-flight; cancel-before-reissue pattern)
- Execution history filter: 8001
- WSH meta: 8010; WSH events per chart instance: 8020–8029
- Multi-position reqId: 8030; Multi-account-update reqIds: 8031–8040
- Smart components per TradingWindow instance: 8050–8059
- Display group query: 8060; Display group subscriptions G1–G4: 8061–8064
- P&L account-wide: 9000; P&L single per-position: 9001–9999

- [x] **Task #40** — Real-time P&L — `MsgPnL { double daily, unrealized, realized }` and `MsgPnLSingle { int reqId, long pos, double daily, unrealized, realized, value }` variants added to `IBMessage`; `IBKRClient` wires `pnl()` / `pnlSingle()` EWrapper callbacks and exposes `reqPnL(reqId, account, modelCode)` / `cancelPnL` / `reqPnLSingle(reqId, account, modelCode, conId)` / `cancelPnLSingle`; `PortfolioData.h` gains `PnLData` struct; `PortfolioWindow` subscribes on open and shows account-level daily/unrealized/realized P&L in a header bar + per-position daily P&L column in the positions table; reqId 9000 for account-wide, 9001–9999 per position

- [x] **Task #41** — Symbol search / autocomplete — `MsgSymbolSamples { int reqId; std::vector<ContractDesc> results }` (`ContractDesc`: symbol, secType, primaryExch, currency); `IBKRClient` wires `symbolSamples()` and exposes `reqMatchingSymbols(reqId, pattern)`; `src/ui/SymbolSearch.h` provides reusable `DrawSymbolInput(label, buf, bufSize, reqMatchFn)` — debounces 300 ms, opens a tooltip-style popup with up to 10 results, arrow-key + Enter to select; applied to ChartWindow toolbar, TradingWindow order entry symbol field, ScannerWindow symbol filter, NewsWindow toolbar; reqId 8000 (cancel-before-reissue). **Validation added:** `SymbolSearchState` tracks `lastConfirmed` and `searchedQuery`; on empty IB results buf reverts to `lastConfirmed`; on focus-lost without confirm reverts to `lastConfirmed` — prevents non-existent symbols from loading data or reaching order submission.

- [x] **Task #42** — Execution history filter — `IBKRClient` exposes `reqExecutions(reqId, ExecutionFilter)` (EWrapper callbacks `execDetails` / `commissionAndFeesReport` already wired; only the call-side is missing); `OrdersWindow` History tab gains a filter toolbar (date-from input, symbol input, side combo, "Load" button) that triggers `reqExecutions`; results append to history list without replacing live-session fills; queried fills shown with a subtle tint to distinguish from live captures; reqId 8001

- [x] **Task #43** — Tick-by-tick data / real Time & Sales — `MsgTickByTick { int reqId; std::string type; double price; long size; std::time_t time; std::string exchange, specialConds }`; `IBKRClient` wires `tickByTickAllLast()`, `tickByTickBidAsk()`, `tickByTickMidPoint()` and exposes `reqTickByTickData` / `cancelTickByTickData`; `TradingWindow` Time & Sales tab replaced with a real tape: ring-buffer (2000 ticks), scrollable table with Time / Price / Size / Exchange / Conditions columns, green/red/grey row tint for uptick/downtick/neutral, size histogram bar column scaled to the max size visible in the current view; reqId range 130–139 (one per TradingWindow instance)

- [x] **Task #44** — WSH Economic Calendar — `MsgWshEvent { int reqId; std::string data }` (IB returns a JSON blob per event); `IBKRClient` wires `wshMetaData()` / `wshEventData()` callbacks and exposes `reqWshMetaData(reqId)` / `cancelWshMetaData` / `reqWshEventData(reqId, filter)` / `cancelWshEventData`; `WshData.h` defines `WshEvent { int conId; std::string date, type, description, importance }` with lightweight hand-rolled JSON field extraction (no external dep); `ChartWindow` populates `m_wshEvents` once conId is known (from existing `contractDetails` flow) and draws vertical dashed markers on the price chart at each event date, colour-coded by type (earnings = yellow, dividend = cyan, split = purple), with a hover tooltip showing type + description; reqId: WSH meta 8010, WSH events per chart instance 8020–8029

- [x] **Task #45** — Multi-account support — `MsgManagedAccts { std::vector<std::string> accounts }`, `MsgPositionMulti { std::string account, modelCode; core::Position pos }`, `MsgAccountUpdateMulti { std::string account, key, val, currency }` added to `IBMessage`; `IBKRClient` wires `managedAccounts()`, `positionMulti()` / `positionMultiEnd()`, `accountUpdateMulti()` / `accountUpdateMultiEnd()` and exposes `reqManagedAccts()`, `reqPositionsMulti` / `cancelPositionsMulti`, `reqAccountUpdatesMulti` / `cancelAccountUpdatesMulti`; `main.cpp` stores `g_managedAccounts`/`g_selectedAccount`; new `ConnectionState::SelectingAccount` defers `FinishConnect()` until account chosen; `RenderAccountSelectorUI()` modal shown during initial connect on multi-account live sessions; single-account sessions auto-select; account shown in menu bar right of DISCONNECTED badge (label for single, clickable dropdown for multi); all placed orders stamped with `order.account = g_selectedAccount` in `PlaceOrder`; `ACCT_SUMMARY_TAGS` expanded to include all financial tags (NetLiquidation, TotalCashValue, BuyingPower, UnrealizedPnL, RealizedPnL) routed to `PortfolioWindow::OnAccountValue`

- [x] **Task #46** — Smart components / exchange routing — `MsgSmartComponents { std::string bboExchange; std::map<int, std::pair<std::string,char>> components }` (bitNumber → exchange + exchange-letter code); `IBKRClient` wires `smartComponents()` and exposes `reqSmartComponents(reqId, bboExchange)`; `SmartRouting.h` defines `SmartRoute { int bit; std::string exchange; char letter; }` and a `g_smartComponents` cache map (bboExchange → vector of SmartRoute) stored in `main.cpp` and refreshed per symbol; `TradingWindow` order entry gains an "Exchange" combo populated from smart components for the current symbol's bboExchange (derived from `tickString` data already flowing in), defaulting to "SMART" then listing IB's actual smart-routing destinations; selected exchange stamped onto `core::Order.exchange`; reqId range 8050–8059

- [x] **Task #47** — IB Display Group sync — `MsgDisplayGroupList { int reqId; std::string groups }` and `MsgDisplayGroupUpdated { int reqId; std::string contractInfo }` (format `"symbol:secType:exchange:conId"`) added to `IBMessage`; `IBKRClient` wires `displayGroupList()` / `displayGroupUpdated()` and exposes `queryDisplayGroups(reqId)`, `subscribeToGroupEvents(reqId, groupId)`, `updateDisplayGroup(reqId, contractInfo)`, `unsubscribeFromGroupEvents(reqId)`; Settings panel gains a "Sync with TWS Display Groups" toggle — when ON, issues `subscribeToGroupEvents` for IB groups 1–4 mapping to our G1–G4; on `displayGroupUpdated`: parse the contractInfo string to extract symbol → call `BroadcastGroupSymbol(groupId, symbol)` (protected by the existing `g_groupSyncInProgress` guard to prevent echo loops); when our own `BroadcastGroupSymbol` fires and sync is ON: call `updateDisplayGroup` on the matching IB group to push the new symbol into linked TWS windows; reqId: query 8060, group subscriptions 8061–8064

- [x] **Task #48b** (unplanned) — Watchlist defaults + saved presets — `WatchlistWindow` opens with Mag 7 (AAPL, MSFT, NVDA, AMZN, GOOGL, META, TSLA) pre-loaded from a built-in "Mag 7" `SavedPreset`; `SpawnWatchlistWindow` calls `AddDefaultsIfEmpty()` after wiring callbacks so subscriptions fire immediately on connect; description enrichment queue (`m_cdQueue` + `m_cdEnrichPending`) requests `reqContractDetails` one per frame to fill in Company column without blocking; static `WatchlistWindow::s_presets` shared across all instances persisted to `~/.config/ibkr-trading-app/watchlist-presets.cfg` (atomic write via `.tmp` + `rename`); preset toolbar row: combo showing active preset name / "Custom", "Save As" button opens modal with name input (updates existing on name match, appends new), "Delete" button removes active preset; `SwitchToPreset(idx)` cancels current subscriptions, loads preset watchlists, re-subscribes, queues enrichment; file format extends existing CSV with `<description>` as col 5 (backward-compatible with zero-col files)

- [x] **Task #48** — Watchlist window — `WatchlistData.h` defines `WatchlistItem { std::string symbol, description, secType, primaryExch, currency; int conId, reqId; double last, bid, ask, change, changePct, volume, bidSize, askSize, lastSize, prevClose, avgVolume, high52w, low52w, high, low, open; bool subscribed; }` and `Watchlist { std::string name; std::vector<WatchlistItem> items; }`; `src/ui/windows/WatchlistWindow.{h,cpp}` multi-instance (up to 10); each instance holds multiple named watchlists accessed via tabs; up to 100 symbols per watchlist; toolbar has group picker (leftmost), named watchlist tabs with + / × buttons to add/rename/remove tabs, "Add Symbol" button, "Columns" button; "Columns" button opens a popup with checkboxes for all 22 columns — Symbol (locked), Company, Last, Chg, %Chg, Bid, Ask, Bid Sz, Ask Sz, Last Sz, Spread, Volume, Avg Vol, High, Low, Open, Prev Cls, 52W Hi, 52W Lo, Type, Exchange, Currency — defaults: first 11 on; `kColDefs[]` table drives column names, widths, defaults; `m_colEnabled[22]` per-instance state; table rebuilds dynamically with only enabled columns; sort maps table column index → kColDefs index so sort works across any visible subset; tick handlers: `OnTickPrice` fields 1,2,4,6,7,9,14,72,73; `OnTickSize` fields 0,3,5,8,21; search popup: `InputText` triggers `reqMatchingSymbols` (debounced 300 ms); on symbol add: `reqContractDetails` then `reqMktData` with genericTicks `"165,233"`; green/red row tint; right-click context menu (Open Chart, Open Order Book, Remove); double-click → `SpawnChartWindow`; reqId layout: contract details 6900–6909; market data 7000–7999 (watchlistIdx × 100 + symbolSlot)

- [x] **Task #49a** (unplanned) — Order status & history improvements — `OrderStatus::PendingCancel` added to enum with `"CANCELLING"` string; `IBKRUtils.h` `ParseStatus` maps IB `"PendingCancel"` to the new value; `onError` handler guards against marking `PendingCancel` orders as Rejected (IB error 10148/10149); `FinishConnect()` now calls `ReqAllOpenOrders()` (fetches orders from all client IDs including previous sessions) and `ReqExecutions(8001)` (fetches today's full fill history) on both initial-connect and reconnect paths; `tests/test_models.cpp` updated with `PendingCancel` → `"CANCELLING"` assertion

- [x] **Task #49** — Watchlist persistence — `main.cpp` gains `WatchlistsFilePath()`, `SaveWatchlistsFile()`, `LoadWatchlistsFromFile()`, `WatchlistSaveBlock` struct; file: `~/.config/ibkr-trading-app/watchlists.cfg` (hand-rolled, no external dep); format per instance block: `INSTANCE:<id>` / `GROUP:<groupId>` / `WATCH:<tabName>` / `<symbol>,<secType>,<primaryExch>,<currency>,<conId>,<description>` CSV rows; `SaveWatchlistsFile()` called at the top of `DestroyTradingWindows()` — guards with `g_watchlistEntries.empty()` so first-connect destroy (before `CreateTradingWindows`) is a no-op; covers disconnect path and app-exit path; `FinishConnect(false)` loads saved blocks after `CreateTradingWindows()`, calls `LoadWatchlists()` on existing window 0 (overriding Mag 7 defaults), spawns additional instances up to `kMaxMultiWin`, restores group assignment via `setGroupId()`; `WatchlistWindow::serialize()` / `LoadWatchlists()` / `CancelAll()` already existed — only `main.cpp` wiring was added

- [x] **Task #50** — TIF expansion

- [x] (unplanned) — `doc-reminder.sh` hook expanded — `PostToolUse` hook now reminds to update root `README.md` for changes to `src/core/services/`, `src/core/models/`, `src/ui/UiScale.h`, `src/ui/windows/`, `src/main.cpp`, and `CMakeLists.txt`; `.claude/rules/task-history.md` added to reminders for `src/core/services/`, `src/core/models/`, `src/ui/windows/`, and `src/main.cpp` (was already on `tests/`)

- [x] **Task #51** — WSH Calendar window — `src/ui/windows/WshCalendarWindow.{h,cpp}` singleton (`g_WshCalendarWindow` in `main.cpp`); `WshCalendarEntry { int conId; std::string symbol, date, type, description, importance; }`; `m_subs` map (conId → `{reqId, symbol}`) with 130-slot bool array for reqId 8070–8199; `SubscribeConId(conId, symbol)` idempotent — called from `onPortfolioUpdate` and `onPositionData` for each position and from chart `contractDetails` handler; `OnWshEvent(reqId, json)` parses with `WshData::ParseWshEvent`, deduplicates by `{conId, type, date}`, appends to `m_events`; `CancelAll()` called by destructor; filter toolbar: symbol `InputText`, From/To popup calendar date pickers (`DrawDatePicker` — button showing "From: YYYY-MM-DD" / "From: Any", popup with prev/next month arrows, 7-col day grid, Clear button; nav state `m_calNavYear[2]`/`m_calNavMonth[2]`), Type combo (All/Earnings/Dividend/Split/Other), Importance combo (All/High/Medium/Low); table: Date | Symbol | Type | Importance | Description — `ImGuiTableFlags_Sortable`, sorted ascending by Date by default; `TypeColor()`: yellow=Earnings, cyan=Dividend, purple=Split, grey=Other; row click → `BroadcastGroupSymbol(1, symbol)`; `onWshEvent` in `main.cpp` routes reqIds 8070–8199 to calendar window alongside existing chart routing (8020–8029); created/destroyed in `CreateTradingWindows`/`DestroyTradingWindows`; added to Windows → IBKR menu and `RenderMainUI`; `WshData.h` is at `src/ui/WshData.h` (not `src/core/services/`)

## Phase 11: Auto Technical Analysis on ChartWindow

Plan lives at `.claude/plans/auto-analysis.md`.

- [x] **Task #52** — Auto S/R detection (Task A of the plan) — `src/core/services/ChartAnalysis.h` introduces pure free functions `FindSwings(highs, lows, k, scanCap=0) -> SwingResult`, `ATR(highs, lows, closes, period) -> vector<double>`, `ClusterLevels(swings, tol) -> vector<Level>`, `KeepTopN(clusters, currentPrice, side, minTouches, maxLevels) -> vector<Level>` with `Swing`, `Level{price,touches,firstIdx,lastIdx}`, `LevelSide{Above,Below}`. `ChartWindow.h` adds `AutoAnalysisSettings` struct (toggles for supports/resistances/trend/donchian/keltner/autoFib/pivotPoints/breakouts plus params swingK=3, trendLookback=50, donchianLen=20, maxLevels=3, minTouches=2, scanCap=1000, trendChannel=false), `using AutoLevel = core::services::Level`, member fields `m_auto`, `m_atr14`, `m_autoSupports`, `m_autoResistances`, and method decls `DetectStructure`, `DrawAutoSupportResistance`, `DrawAutoSettingsPopup`. `ChartWindow.cpp`: `DetectStructure()` runs at the tail of `ComputeIndicators()` so every existing recompute trigger refreshes auto-analysis (early-out if `m_closes.size() < 30`); `tol = max(0.3% * lastClose, 0.5 * ATR14)`; `DrawAnalysisToolbar()` gains an "Auto:" sub-section after the drawing-tool buttons with `Sup`/`Res`/`AutoTrend` checkboxes (toggling Sup/Res calls `DetectStructure()` directly) and an `Auto...` small-button that opens `DrawAutoSettingsPopup()` (`InputInt` for swingK 1–20, minTouches 1–10, maxLevels 1–10, scanCap ≥ 0; any change re-runs detection); `DrawAutoSupportResistance()` is invoked from `DrawOverlays()` between the current-price line and the user drawings — red dashed h-lines (alpha = `min(255, 140 + touches*25)`) for resistance, green for support, **left-edge** price tag formatted ` R 187.42 (3×) ` (UTF-8 multiplication sign) so it doesn't collide with the right-edge current-price tag. `tests/test_chart_analysis.cpp` (16 cases / 58 assertions) covers `FindSwings` (V/W/flat-top/scanCap), `ATR` (constant TR, too-small input), `ClusterLevels` (empty, within/outside tol, ascending order), `KeepTopN` (side+touches filter, recency tie-break, no-cap mode), and an end-to-end V-shape pipeline assertion. Added to `tests-core` source list. AutoTrend toggle is wired but rendering is deferred to Task #53 (Task B in the plan).

- [x] **Task #53** — Auto trend line (Task B of the plan) — `core::services::LinearRegression(closes, lookback) → TrendFit{valid, slope, intercept, sigma, firstIdx, lastIdx}` added as inline free function (closed-form OLS using bar idx as x; sigma is population residual stdev). `ChartWindow.h` aliases `using AutoTrend = core::services::TrendFit;` and gains `m_autoTrend` plus the `DrawAutoTrend()` decl. `DetectStructure()` populates `m_autoTrend` whenever `m_auto.trend` is on (using `min(trendLookback, n)` so short series still fit). `DrawAutoTrend()` is invoked from `DrawOverlays()` after S/R rendering: solid segment from `(firstIdx, y(firstIdx))` to `(lastIdx, y(lastIdx))` plus a faded (60% alpha) `L/4`-bar forward projection; colour reflects slope direction (green if `slope > +ε`, red if `< −ε`, grey otherwise) with `ε = 0.05·sigma/L`; when `m_auto.trendChannel` is on, ±2σ parallel lines render on both segments at 50% alpha (and fainter on the projection); a right-edge ` trend +0.123/bar ` label tags the projection endpoint. `DrawAnalysisToolbar()` `AutoTrend` checkbox now triggers `DetectStructure()`; `DrawAutoSettingsPopup()` gains `Trend lookback` (5–500) and `Show ±2σ channel` controls. `tests/test_chart_analysis.cpp` adds 5 cases / 22 assertions covering exact recovery on `y = 2x + 5`, flat line slope=0, lookback < n trailing-window selection, period-4 unbiased noise (slope=1, sigma=1), and too-small/empty input → `valid=false` (now 22 cases / 98 assertions in `[analysis]`).

- [x] **Task #54** — Supply/demand zones + imminent-breakout signal (Task D of the plan) — `core::services::Level` extended with `minPrice`/`maxPrice` (lowest/highest constituent swing price). `ClusterLevels` now populates them as it sweeps the sorted swings. `ChartWindow::AutoAnalysisSettings` gains `bool zones = false`; `enum class BreakoutDirection { None, LongSetup, ShortSetup }` plus members `m_breakoutSignal`/`m_breakoutZoneTop`/`m_breakoutZoneBot`/`m_breakoutFromSupply` track the active signal. `ComputeBreakoutSignal()` runs at the tail of `DetectStructure()` when `zones` is on (after S/R detection): walks supply zones first then demand zones, finds the one whose `[minPrice − 0.5·ATR, maxPrice + 0.5·ATR]` interval contains `closes[n−1]`, then requires Bollinger-Band compression (`bbWidth[n-1] < 0.7 × avg(bbWidth[n-50..n-1])` with ≥20 valid samples) AND directional momentum (`closes[n-1] - mean(closes[n-6..n-2])` exceeds ±0.1·ATR). `LongSetup` when `last > midZone` and bullish; `ShortSetup` when `last < midZone` and bearish. `DetectStructure()` updated to populate the S/R clusters whenever `zones` is on, even if the individual `supports`/`resistances` toggles are off. `DrawAutoZones()` invoked from `DrawOverlays()` between the current-price line and `DrawAutoSupportResistance()` so dashed S/R lines render on top of the rectangles: translucent red rectangle + thin red border for supply zones, translucent green for demand. When a signal is active, draws a coloured triangle on the right edge inside the plot rect at `midZone` y-coordinate (▲ green for long, ▼ red for short) with a ` LONG SETUP ` / ` SHORT SETUP ` label tag to its left. `DrawAnalysisToolbar()` gains a `Zones` checkbox in the Auto: row (default OFF; toggling re-runs `DetectStructure()`). `tests/test_chart_analysis.cpp` extended (now 17 cases / 76 assertions) with new cases for `minPrice`/`maxPrice` from mixed-input swings, single-swing min==max==price invariant, and the V-shape end-to-end test now asserts every cluster's min/max agrees with its actual swing constituents.

- [x] **Task #55** — Five extras (Task C of the plan) — Donchian + Keltner + auto-Fibonacci + classic daily pivots + breakout markers, all toggleable from the chart's Auto: toolbar row (defaults OFF). `core::services::ChartAnalysis.h` gains four pure helpers: `DonchianBands(highs, lows, N) → DonchianResult{hi, lo}` (rolling N-bar max/min, 0-prefix before period); `LargestSwingSpan(swingHighs, swingLows, window) → AutoFibSpan{valid, hiIdx, hiPrice, loIdx, loPrice}` (picks the max-span pair among the last `window` swings of each list — auto-fib anchor); `ClassicPivots(prevH, prevL, prevC) → DailyPivot{valid, p, r1-r3, s1-s3}` (P=(H+L+C)/3, R/S formulas); `FindBreakouts(resistances, supports, highs, lows, closes, atr, lookback, minTouches) → vector<BreakoutMark{idx, y, up}>` (walks last `lookback` bars; up mark when `close[i-1] <= R.price < close[i]`, mirror for support; uses cluster.firstIdx as causality guard; minTouches filter rejects weak levels). `ChartWindow.h` aliases `AutoFibSpan`/`DailyPivot`/`BreakoutMark` from `core::services` and adds members `m_donchHi`/`m_donchLo`/`m_keltUpper`/`m_keltLower`/`m_autoFib`/`m_pivots`/`m_pivotsTodayStart`/`m_pivotsTodayEnd`/`m_breakouts`. `AutoAnalysisSettings` already had `donchian`/`keltner`/`autoFib`/`pivotPoints`/`breakouts` toggles. `DetectStructure()` extended: clears all new fields up-front; populates Donchian/Keltner unconditionally when their toggles are on (Keltner reuses `m_ema` which `ComputeIndicators` always computes regardless of the EMA20 indicator toggle); calls new private helper `ComputeDailyPivots()` (walks `m_xs` backwards using `localtime` to find the previous trading day's OHLC, computes pivots, pins today's idx range) when `pivotPoints && IsIntraday(m_timeframe)`; runs `FindSwings` once for any of S/R/zones/autoFib/breakouts; populates `m_autoFib` via `LargestSwingSpan(sw.highs, sw.lows, 30)` and `m_breakouts` via `FindBreakouts(highClusters, lowClusters, ...)` reusing the unfiltered cluster output. Rendering: `DrawDonchian()` and `DrawKeltner()` plot two ImPlot lines each (faded yellow / faded cyan, named `Donch Hi`/`Donch Lo`/`Kelt Hi`/`Kelt Lo` for the legend), invoked from `DrawCandleChart()` before candlesticks; `DrawBreakoutMarks()` uses two `ImPlot::PlotScatter` calls with `ImPlotMarker_Up`/`ImPlotMarker_Down` after candlesticks/overlays; `DrawAutoFib()` and `DrawAutoPivots()` invoked from `DrawOverlays()` after S/R + trend, before user drawings — fib uses faded `kFibColors` and left-edge labels (so it doesn't collide with the right-edge current-price tag), pivot levels are 7 dashed lines spanning `[m_pivotsTodayStart, m_pivotsTodayEnd]` with right-aligned label tags inside today's range. `DrawAnalysisToolbar()` gains five new checkboxes (`Donch`, `Kelt`, `AutoFib`, `Pivots`, `Breakouts`) in the Auto: row; the Pivots checkbox is wrapped in `BeginDisabled()`/`EndDisabled()` on D1/W1/MN with an `AllowWhenDisabled` tooltip ("Intraday only — pivots use the prior day's OHLC."). `DrawAutoSettingsPopup()` gains a Donchian section with a `Donchian length` `InputInt` (2–200, default 20). `tests/test_chart_analysis.cpp` adds 15 new cases / 100 new assertions across `[donchian]`/`[autofib]`/`[pivots]`/`[breakout]` tags (now 37 cases / 176 assertions in `[analysis]`).

## Phase 12: Setup Suggestions & Position Safety on ChartWindow / TradingWindow

Plan lives at `.claude/plans/setup-suggestions.md`. Two related additions on top of the Phase 11 auto-analysis layer: a structure-based **setup overlay** that surfaces an entry/stop/target reference plan when the existing `m_breakoutSignal` fires, and an always-on **unguarded-position guard** that warns when the user holds a position with no protective Stop/StopLimit/Trail/TrailLimit on the same symbol. Both reuse the same SL-hunter defenses (padding past the structural anchor, longest-wick anchoring via `Level.minPrice`/`maxPrice`, round-number avoidance, Stop-Limit offset). All trade-staging paths (entry leg from "Use suggestion", protective stop from "Place stop") route through the existing per-window confirmation modals — never bypass confirmation regardless of any "Transmit Instantly" toggle.

- [x] **Task #56** — Pure helpers + tests (Task A of the plan) — `core::services::ChartAnalysis.h` gains four inline free functions: `AvoidRoundNumber(price, pad, pushDown) → double` (snap a price away from the nearest .00/.25/.50/.75/1.00 mark by at least `pad`; `pushDown=true` for stops below entry, false for stops above); `SuggestSetup(side, zoneTop, zoneBot, anchor, opposingLevel, atr, last, atrPad, roundPad, stopOffset, rrMin, equity, riskPct) → SetupPlan{valid, side, entry, stop, stopLmt, target, rr, shares, refLevelIdx}` (long path uses `last` as entry when past the supply zone or zone midpoint inside, then `rawStop = anchor − atrPad·atr` snapped via `AvoidRoundNumber(roundPad, pushDown=true)`, target = nearest opposing level, R:R must clear `rrMin` else `valid=false`; short mirrors with anchor=`Level.maxPrice`, `pushDown=false`, target=nearest support); `SuggestStopForPosition(isLong, entry, levels, atr, atrPad, roundPad, stopOffset) → PositionStop{valid, stop, stopLmt, pctRisk}` (filters levels by side+`touches≥2`, picks the one closest to entry, anchors at its `minPrice`/`maxPrice`, applies the same padding+round-snap pipeline); `PositionSizeShares(equity, riskPct, entry, stop) → int` (`floor(riskPct/100 × equity / |entry-stop|)`; 0 on degenerate input). The `anchor` parameter to `SuggestSetup` was renamed from the originally-spec'd `anchorMin` so a single name covers both sides — caller passes `Level.minPrice` for long, `Level.maxPrice` for short. Added `<limits>` include for the `numeric_limits` use in `AvoidRoundNumber`. `tests/test_chart_analysis.cpp` adds 13 new cases / 55 assertions under the `[setup]` tag covering: `AvoidRoundNumber` (already-safe prices, push down/up at $10.00 and $187.50, near-1.00 cross-integer push, degenerate `pad≤0` / `price≤pad` / sub-cent pad), `SuggestSetup` long inside-zone (entry=mid=186, stop=183.43 snapped past .50, R:R≈2.34, shares=0 when equity=0) and `last>zoneTop` (entry clamps to last=188, shares=218 with $100k×1%), R:R rejection (opposingLevel=187.5 → reward 1.5 < 2.57 risk → `valid=false`), short mirror (anchor=`zone.maxPrice`=190.5, stop=191.57, target=180, R:R≈3.5), degenerate input (atr=0, zoneTop≤zoneBot, side=2) → invalid, `SuggestStopForPosition` long picks closer cluster, single-touch/empty-list/no-qualifying-level rejection, short mirror (picks 185-cluster as nearest resistance), `PositionSizeShares` basic math (1% of $100k / $3 risk → 333; 0.5% of $50k / $2.50 → 100) and degenerate (zero/negative equity, zero risk%, entry==stop) → 0. `[setup]` tag: 13 cases / 55 assertions; `[analysis]` umbrella tag (setup + analysis combined): 50 cases / 231 assertions; tests-core overall 100/100 cases pass.

- [x] **Task #57** — Setup overlay + Use suggestion button (Task B of the plan) — `ChartWindow.h` gains `SetupSettings { overlay=false, rrMin=2.0, atrPad=0.5, roundPad=0.07, stopOffset=0.10, riskPct=1.0, useStopLmt=true }`, `core::services::SetupPlan m_setup` (aliased as `SetupPlan`), and `m_breakoutLevelIdx` (idx into `m_autoSupports`/`m_autoResistances` for the active zone — needed because the existing breakout signal only stored the buffered top/bottom of the active zone, not which `Level` produced it; `ComputeSetupPlan()` needs the underlying `Level` to read its `minPrice`/`maxPrice` for the structural anchor). New decls: `ComputeSetupPlan`, `DrawSetupOverlay`, `DrawSetupSettingsPopup`. New public accessor `AutoLevelSnapshot getAutoLevels() const` returning `{m_autoSupports, m_autoResistances, m_atr14.empty() ? 0.0 : m_atr14.back()}` so main.cpp can build per-chart position-stop suggestions in Task #58. `ChartWindow.cpp`: forward-declares `extern double GetSelectedAccountEquity()`; `ComputeBreakoutSignal()` now records `m_breakoutLevelIdx`; `ComputeSetupPlan()` invoked at the tail of `DetectStructure()` when `m_setupSettings.overlay && m_breakoutSignal != None` — picks the right anchor side (long → demand zone `minPrice`, short → supply zone `maxPrice`), finds the nearest opposing level by walking `m_autoResistances`/`m_autoSupports`, fetches equity, calls `SuggestSetup()`. `DetectStructure()` resets `m_setup = SetupPlan{}` and `m_breakoutLevelIdx = -1` up-front so toggling the overlay off cleanly clears state. `DrawOverlays()` invokes `DrawSetupOverlay()` between `DrawAutoSupportResistance()` and `DrawAutoTrend()` — three dashed h-lines (entry cyan ` ENTRY 187.42 x N sh `, stop red ` STOP 184.97 (-1.31%) `, target green ` TGT 192.10 (R:R 2.1) `, all right-edge labels so they don't collide with left-edge S/R tags). Faint stop-limit companion line rendered when `m_setupSettings.useStopLmt`. `DrawAnalysisToolbar()` gains a `Setup` checkbox after `Breakouts` in the Auto: row (default OFF). `DrawAutoSettingsPopup()` appends `DrawSetupSettingsPopup()` after the Donchian section: R:R minimum (1.0–5.0), ATR padding (0.1–2.0), Round-number pad ($) (0.0–0.50), Stop-limit offset ($) (0.0–1.0), Risk per trade (%) (0.1–5.0), `Use Stop-Limit (off = plain Stop)` checkbox; any change re-runs `DetectStructure()`. `DrawTradePanel()`: when `m_setupSettings.overlay && m_setup.valid`, a blue `[Use suggestion]` button renders right after the SELL button — click forces `m_orderTypeIdx = 1` (Limit), adopts `m_setup.shares` as the qty when known, builds a Limit order via the existing `buildOrder()` lambda, sets `o.limitPrice = m_setup.entry`, stages it in `m_pendingConfirmOrder`, and opens `DrawConfirmPopup()` regardless of the Transmit Instantly toggle. Tooltip explains "Reference plan, not advice." `PortfolioWindow.h` gains `[[nodiscard]] double netLiquidation() const`; `main.cpp` adds free function `double GetSelectedAccountEquity()` returning `g_PortfolioWindow ? g_PortfolioWindow->netLiquidation() : 0.0`. Wiring-only — no new pure-logic test cases (the helpers under `[setup]` were already covered in Task #56).

- [x] **Task #58** — Unguarded-position guard (Task C of the plan) — `main.cpp` adds `struct UnguardedPosition { string symbol; long conId; double qty, avgCost; }` and `static std::vector<UnguardedPosition> g_unguarded`. `RecomputeUnguardedPositions()` walks `g_positions × g_liveOrders` matching by `(symbol, opposite side, live status, protective type, qty>0)` — protective types are `Stop`/`StopLimit`/`Trail`/`TrailLimit`; live statuses are `Pending`/`Working`/`PartialFill`/`PendingCancel`. Wired into `onPositionData` (line 1671), `onPortfolioUpdate` (line 1698), `onOpenOrder` (line 1706), `onOrderStatusChanged` (line 1727); each adds the call after the existing logic — re-entrant-safe, no IB calls. `onPositionData` also mirrors `pos` into `g_positions` so the guard sees the same single source of truth `onPortfolioUpdate` populates (quantity-zero erases the entry so the warning self-clears on flat). `PushUnguardedHintsToWindows()` is called once per frame from `RenderTradingUI()` right before window `Render()` calls — for each chart, builds the hint from its own `getAutoLevels()` + `SuggestStopForPosition()` (using fixed defaults `atrPad=0.5`, `roundPad=0.07`, `stopOffset=0.10` — the per-chart `SetupSettings` knobs are reserved for the overlay path; the warning uses safe constants so all charts agree); for each trading window, borrows S/R from the *first* chart on the same symbol — falls back to `active=false` (no warning) when no chart instance is open for the symbol (v1 behaviour per plan §3f). Cleared hints (`active=false`) are sent to all non-matching windows so stale strips clear automatically. Both `ChartWindow` and `TradingWindow` gain an identical `UnguardedHint { active, symbol, qty, avgCost, stopTrig, stopLmt, pctRisk }` struct (declared independently to avoid header coupling), `m_unguarded`/`m_dismissedUnguarded` (`std::unordered_set<std::string>` of dismissed symbols)/`m_lastWarnedQty` private state, `SetUnguardedSuggestion(const UnguardedHint&)` slot (quantity-change resets the per-symbol dismissal so the strip re-appears after the user trims/adds), and `DrawUnguardedStrip()` rendering a yellow strip with FlexRow toolbar — early-out on `!active`, symbol mismatch, dismissed symbol, or `stopTrig <= 0` so partially-populated hints self-suppress. `ChartWindow::DrawUnguardedStrip()` "Place stop" stages a `core::Order` (StopLimit if `m_setupSettings.useStopLmt`, else plain Stop) on the opposite side of the position, full quantity, DAY/RTH-only, routed through `DrawConfirmPopup()` — never bypasses confirmation. `TradingWindow::DrawUnguardedStrip()` pre-fills the order-entry buffers (`m_sideIdx`, `m_typeIdx=3`, `m_tifIdx=0`, `m_outsideRth=false`, `m_qtyBuf`, `m_stpBuf`, `m_lmtBuf`) and triggers `m_showConfirm = true` so the existing `DrawConfirmationPopup()` reads the right values. The strip is rendered as a `BeginChild`-wrapped band (`ImGuiChildFlags_Borders`) above `DrawInfoBar()`/`DrawPositionStrip()`/`DrawCandleChart()` on chart, and inside `##entry_panel` above `DrawOrderEntry()` on TradingWindow. Build clean, 116/116 tests pass; live IB smoke-test deferred (manual). Live-IB acceptance steps documented in `.claude/plans/setup-suggestions.md` Task C section.

- [x] (unplanned, 2026-04-27) — **IB error 110 regression fix** for Tasks #56 / #58. `Place stop` was rejected by IB with error 110 ("price does not conform to the minimum price variation for this contract") because `AvoidRoundNumber`'s `floor + (nearest - pad)` arithmetic leaks ULP-level FP drift on inputs like `0.07` (e.g. `184 - 0.07 = 183.92999999…`), and the same drift propagates into `stopLmt = stop ± offset`. Fixed by adding `core::services::RoundToTick(price, tick=0.01)` to `ChartAnalysis.h` (`std::round(price/tick) * tick`; `tick<=0` is a no-op) and applying it to every output price in `SuggestSetup` (entry/stop/stopLmt/target) and `SuggestStopForPosition` (stop/stopLmt) before stamping into the result struct. `pctRisk` in `SuggestStopForPosition` is now computed against the rounded stop so the displayed % matches the submitted price exactly. `tests/test_chart_analysis.cpp` adds 4 new cases / 25 new assertions under `[setup]`: `RoundToTick` basic ($0.01 grid), custom ticks ($0.05 / $0.0001), `tick<=0` no-op, and a regression case `SuggestStopForPosition: outputs are tick-clean (regression for IB error 110)` that exercises both long and short paths through the AvoidRoundNumber drift case and asserts `value*100 == round(value*100)` for stop / stopLmt / entry / target. `[setup]` tag now: 17 cases / 80 assertions; `tests-core` overall 120/120 pass.

## Phase 13: Trading-Style Modes + VWAP Bands on ChartWindow

Plans live at `.claude/plans/trading-styles.md` and `.claude/plans/vwap.md`. Two coupled additions: (1) extract VWAP into a pure helper and add ±1σ/±2σ volume-weighted bands; (2) replace the "TF combo + free-floating settings" model with four curated **Trading Style** presets (Scalping / Day Trading / Swing / Investment) that hard-bind {timeframe + history horizon + auto-analysis params + setup-overlay params + indicator/setup defaults} so the auto S/R, breakout signal, setup overlay, and unguarded-stop guard stay coherent within a session. The two plans share fields: `StylePreset.indVwap` and `StylePreset.indVwapBands` feed the per-mode VWAP defaults, so they ship together.

- [x] **Task #59** — VWAP primitive + ±σ bands tests (Task A of `vwap.md`) — `core::services::ChartAnalysis.h` gains `VwapResult { vwap, sd1Up, sd1Dn, sd2Up, sd2Dn }` and `SessionVwap(highs, lows, closes, volumes, sessionStarts) → VwapResult` as inline free function. Single-pass closed-form `Var[X] = E[X²] − E[X]²` formulation: tracks running `cumTPV`/`cumVol`/`cumTPV2`, resets on every index in `sessionStarts` (and at `i==0`); zero-volume bars carry the previous values forward; first-bar zero-volume falls back to `closes[0]`. `tests/test_chart_analysis.cpp` adds 6 new cases / 84 assertions under the `[vwap]` tag covering: known volume-weighted average across three bars (typical=`{100,102,101}`, vol=`{1k,2k,3k}` → vwap[2]≈101.166); single-bar input → vwap==typical / bands collapsed; session reset wipes carry-over; zero-volume bar carry-forward; empty / mismatched-length input → zero-filled / no crash; and band-ordering invariant (`sd2Dn ≤ sd1Dn ≤ vwap ≤ sd1Up ≤ sd2Up` element-wise on a varying series).

- [x] **Task #60** — VWAP bands ChartWindow integration (Task B of `vwap.md`) — `IndicatorSettings::vwapBands` (default false) added next to `vwap`. New member vectors `m_vwapSd1Up`/`m_vwapSd1Dn`/`m_vwapSd2Up`/`m_vwapSd2Dn` populated alongside `m_vwap`. `ChartWindow::CalcVWAP` static removed — `ComputeIndicators()` now builds `sessionStarts` from `m_xs` (intraday: detect ET-day boundary via `localtime`; daily/weekly/monthly: empty list = single cumulative run from index 0) and calls `core::services::SessionVwap(...)`. `DrawCandleChart()` renders four faded-gold band lines (`VWAP+1σ`, `VWAP-1σ` at alpha=0.30; `VWAP+2σ`, `VWAP-2σ` at alpha=0.18) when `m_ind.vwap && m_ind.vwapBands && m_vwapSd2Up.size() == n`; legend entries match. `DrawToolbar()` appends a compact `±σ` checkbox right after the existing `VWAP` checkbox, gated on `m_ind.vwap` (only renders when VWAP is on); tooltip "Show ±1σ and ±2σ volume-weighted bands around VWAP." `RequestNewData()` and `setTradingStyle()` clear all five vectors so a TF / mode switch starts from scratch. Build clean, manual smoke-test deferred to live IB session.

- [x] **Task #61** — TradingStyle presets + ApplyPreset + tests (Task A of `trading-styles.md`) — `src/core/services/TradingStyle.h` (new file) declares `enum class TradingStyle : int { Scalping=0, DayTrading=1, Swing=2, Investment=3 }`, label helpers `TradingStyleLabel` (full name with space) / `TradingStyleShort` (single-word badge), `StylePreset` POD struct bundling `{timeframe, historyDuration, <auto>, indVwap/indVwapBands, <setup>}`, `GetPreset(s)` returning the four canonical presets (Scalping = M1 / 2 D / scalper params; Day Trading = M15 / 20 D / VWAP+bands ON; Swing = D1 / 1 Y / autoFib ON; Investment = W1 / 5 Y / wide pad / no zones), and a templated `ApplyPreset(preset, ind, a, s, tf)` helper that stamps every overridable field onto caller-supplied settings structs (templated so tests can instantiate with hand-rolled stub structs that have the same field names — keeps `TradingStyle.h` free of `ChartWindow.h` dependency). `tests/test_chart_analysis.cpp` adds 7 new cases / 114 assertions under the `[style]` tag: label/short coverage, full field-by-field assertions on each of the four presets (timeframe / historyDuration / `swingK` / `setupOverlay` / `riskPct` / `useStopLmt` / `indVwap` / `indVwapBands`), full `ApplyPreset` stub-struct stamping, and a round-trip case proving Scalping → Investment leaves no carry-over field. Total `[analysis]` umbrella: 67 cases / 454 assertions; tests-core overall 117 cases / 688 assertions.

- [x] **Task #62** — ChartWindow trading-style integration (Task B of `trading-styles.md`) — `ChartWindow.h` gains `#include "core/services/TradingStyle.h"`, `core::services::TradingStyle m_tradingStyle = ::Swing;` member, `tradingStyle() const` accessor, `setTradingStyle(s, silent=false)` mutator, and `OnStyleChange(style, historyDuration, useRTH)` callback. `ChartWindow.cpp` `setTradingStyle()`: applies preset via `ApplyPreset`, wipes every derived buffer (`m_xs`/`m_idxs`/`m_opens`/`m_highs`/`m_lows`/`m_closes`/`m_volumes`/`m_sma1/2`/`m_ema`/`m_bbMid/Upper/Lower`/`m_rsi`/`m_vwap`+bands/`m_atr14`/`m_autoSupports/Resistances`/`m_autoTrend`/`m_donchHi/Lo`/`m_keltUpper/Lower`/`m_autoFib`/`m_pivots`/`m_breakouts`/`m_breakoutSignal`/`m_setup`/`m_unguarded`/`m_drawings`), sets `m_loading=true`/`m_hasRealData=false`/`m_viewInitialized=false`, fires `OnStyleChange(s, preset.historyDuration, m_useRTH)` unless `silent=true`. `DrawToolbar()` adds a Style combo before the timeframe display (the TF combo is now read-only — set via Style); `±σ` checkbox added next to VWAP. `silent=true` is used by the chart-modes.cfg load path so connect doesn't enqueue N redundant IB requests.

- [x] **Task #63** — main.cpp mode-switch queue + duration override (Task C of `trading-styles.md`) — `main.cpp` adds `<deque>` include; `struct PendingStyleSwitch { int chartIdx; TradingStyle style; std::string historyDuration; bool useRTH; }` with `static std::deque<PendingStyleSwitch> g_pendingStyleSwitches;`, `g_nextStyleSwitchAllowed = 0.0`, `kStyleSwitchThrottleSec = 1.0`. `ReqChartData()` gains a `const std::string& durationOverride = ""` parameter — empty falls back to `core::TimeframeIBDuration(tf)` (existing behaviour); non-empty passes through to `ReqHistoricalData`. New `DrainStyleSwitchQueue()` pops at most one entry per second from the FIFO, cancels the chart's in-flight `extId` extend-history request (so older bars from the previous TF can't land into the new series), reads symbol/useRTH at drain time (not enqueue time, so a symbol change between click and drain doesn't strand the wrong contract), and re-issues `ReqChartData(...)` with the preset's duration. `SpawnChartWindow()` wires `e.win->OnStyleChange = [idx](style, historyDuration, useRTH) { drop any prior queue entry for this chartIdx (latest-wins so spamming the combo doesn't walk through every intermediate mode); push the new entry; mark g_chartModesDirty }`. `RenderTradingUI()` calls `DrainStyleSwitchQueue()` once per frame next to `PushUnguardedHintsToWindows()`. Throttle math: 1 req/sec × 10 charts = 10 s spacing on bulk-mode-switch, well under IB's 60-req/10-min/contract pacing limit.

- [x] **Task #64** — chart-modes.cfg persistence (Task D of `trading-styles.md`) — `main.cpp` adds `ChartModesFilePath()` returning `~/.config/ibkr-trading-app/chart-modes.cfg` (mirrors `WatchlistsFilePath()`); `ChartModeBlock { instanceIdx, symbol, style }`; `g_chartModesDirty` (set in `OnStyleChange`) and `g_lastChartModesSave` flag-pair declared up-front so the lambda can capture them without forward-declaration tricks. `SaveChartModesFile()` (atomic `.tmp` + `rename`) walks `g_chartEntries` and writes one block per chart with a non-empty symbol: `INSTANCE:<idx>` / `SYMBOL:<sym>` / `STYLE:<int>`. `LoadChartModesFromFile()` parses the same format. `RenderTradingUI()` flushes once per second when `g_chartModesDirty`. `DestroyTradingWindows()` flushes synchronously when dirty (covers disconnect / app-exit). `FinishConnect(false)` post-`CreateTradingWindows()`, post-watchlist-load: walks each saved block, calls `setTradingStyle(style, silent=true)` (clears state without enqueueing through the throttle queue), then `SetSymbol(b.symbol)`, then `ReqChartData(... preset.historyDuration)` with `useRTH = !isIntraday(preset.timeframe)` matching the m_useRTH=false default for fresh charts; tracks restored chart indices in `restoredCharts[]` so the AAPL D1 fallback seed for chart 0 is gated on `!restoredCharts[0]`. Style serialization uses the integer enum value (most robust to label changes); `LoadChartModesFromFile()` clamps to `[0, Investment]` to reject corrupt data. `g_chartModesDirty=false` after load (loading from disk is not a user-initiated change). Build clean, 117/117 tests pass; live kill-and-restart smoke-test deferred (manual).

- [x] **Task #65** — Free trading-style mode (unplanned, 2026-05-01) — `core::services::TradingStyle::Free = 4` added to the enum, with matching `TradingStyleLabel`/`TradingStyleShort` cases (`"Free"` / `"FREE"`). `GetPreset(Free)` returns the construction-default baseline of `IndicatorSettings`/`AutoAnalysisSettings`/`SetupSettings` (placeholder `Timeframe::D1` + `"6 M"` duration). `ChartWindow::setTradingStyle(Free, silent)` saves `m_timeframe` before `ApplyPreset` and restores it afterward (Free's whole purpose is to let the user pick any TF), and the `OnStyleChange` duration is computed dynamically from the preserved TF via `core::TimeframeIBDuration` rather than the static preset value. New public `setTimeframeFree(tf, silent=false)` method updates `m_timeframe`, wipes data buffers + derived analysis state but **preserves user settings** (`m_ind`/`m_auto`/`m_setupSettings`/`m_drawings`), and fires `OnStyleChange(Free, TimeframeIBDuration(tf), m_useRTH)`. `ChartWindow::DrawToolbar()` style combo gains `"Free"` as the 5th entry; the timeframe widget conditionally renders an editable Combo (populated from `kAllTimeframes[]`) when `m_tradingStyle == Free`, otherwise stays as the disabled `[%s]` label. `main.cpp` `DrainStyleSwitchQueue()` recomputes `duration = TimeframeIBDuration(getTimeframe())` for Free-mode entries at drain time so a TF change between enqueue and drain can't strand the wrong duration. `chart-modes.cfg` format gains an optional `TF:<int>` line written only when `STYLE==Free`; `LoadChartModesFromFile` parses it (clamps to `[M1, MN]`), the load path applies it via `setTimeframeFree(savedTF, silent=true)` after `setTradingStyle(Free, silent=true)`, then issues `ReqChartData` with `tf` + `TimeframeIBDuration(tf)` and `useRTH = !isIntraday(tf)`; old configs without `TF:` lines remain compatible (default `timeframe = -1` = use preset's TF). `LoadChartModesFromFile` `STYLE` clamp widened from `[0, Investment]` to `[0, Free]`. `tests/test_chart_analysis.cpp` adds 3 new cases under `[style]` (TradingStyleLabel/Short Free coverage merged into the existing five-modes case; new `GetPreset(Free)` field-by-field assertions matching `IndicatorSettings`/`AutoAnalysisSettings`/`SetupSettings` defaults; new `ApplyPreset(Free)` round-trip from Investment proving every overridable field is overwritten back to baseline). 135/135 tests pass.

- [x] **Task #66** — Order Impact — side-intent badge + PnL preview (plan at `.claude/plans/order-impact.md`) — `core::services::ChartAnalysis.h` gains `OrderImpactKind` enum (OpenLong/OpenShort/AddToLong/AddToShort/ReduceLong/ReduceShort/CloseLong/CloseShort/FlipToShort/FlipToLong/Invalid), `OrderImpact` struct, `StopTargetPreview` struct, and two pure inline helpers: `ComputeOrderImpact(posQty, avgCost, commissionPerShare, isBuy, orderQty, fillPrice) → OrderImpact` and `PreviewStopTarget(target, stop) → StopTargetPreview`. `ChartWindow` and `TradingWindow` each gain a `DrawOrderImpactBadge()` private method rendering a colored `BeginChild` strip below the order form — blue for Open/AddTo, green/red for Reduce/Close (profit/loss), orange for Flip. Second-line target/stop preview with R:R ratio when the Phase 12 setup overlay is active. Fill-price derivation is per order type (last/limitPrice/stopPrice/trail estimate/peg estimate). Existing closing-leg PnL lambdas in ChartWindow (`calcOrderPnL`, `calcArmedPnL`) refactored to call `ComputeOrderImpact` (de-duplicates the PnL formula and fixes commission to per-share). `TradingWindow.cpp` gains `#include "core/services/ChartAnalysis.h"`. `tests/test_chart_analysis.cpp` adds 17 new cases / 74 assertions under `[order-impact]` tag. Architecture, testing, and task-history docs updated. Build clean, 136/136 tests pass; live IB smoke-test deferred (manual).

- [x] (unplanned, 2026-05-02) — **Multi-viewport modal freeze fix** in `WatchlistWindow`. With `ImGuiConfigFlags_ViewportsEnable` on, dragging the Watchlist out of the dockspace and past the main GLFW window's edges spawns a separate OS-level GLFW window via `ImGui_ImplGlfw_CreateWindow`. The `Save Preset##wlsavepop` modal at `WatchlistWindow.cpp:693` lacked an explicit `SetNextWindowPos`, so on `Save As` it defaulted to the **main viewport's** centre — invisible to a user looking at the undocked window — while still capturing all ImGui input as a popup-modal does. Symptom: the undocked watchlist (and every other window) appeared frozen, couldn't be clicked, dragged, or closed until the user re-focused the main GLFW window and dismissed the hidden modal. Fix mirrors the pattern already documented at `TradingWindow.cpp:1454-1458` and applied at `ChartWindow.cpp:1772-1774`: `ImGui::SetNextWindowPos(ImGui::GetWindowViewport()->GetCenter(), ImGuiCond_Always, ImVec2(0.5, 0.5))` + `SetNextWindowSize(ImVec2(em(260), 0), ImGuiCond_Always)` immediately before the `BeginPopupModal` call, so the modal opens centred on whichever viewport the watchlist itself currently lives in. `ChartWindow` and `TradingWindow` modals were already viewport-aware; these are the only three `BeginPopupModal` call sites in the codebase, so all three now share the same protection.

- [x] (unplanned, 2026-05-02) — **Edge-resize cross-viewport fix** in `HandleWindowResize` (`main.cpp`).

## Phase 14: Replay Window — Pre / Intra / Post-Market Playback

Plan at `.claude/plans/replay.md`. All tasks A–H shipped May 2026.

- [x] **Task #67** — ReplayData model (`src/core/models/ReplayData.h`) — `TickType` enum (Trades/BidAsk/Midpoint), `HistoricalTick` POD (per-stream fields), `HistoricalDay` struct (symbol, date, bars, ticks, userFills).

- [x] **Task #68** — ReplayEngine pure-logic core + tests (Task A) — `src/core/services/ReplayEngine.h` (720 lines, header-only): `ReplaySession` enum + labels, `ReplayClock` + `Tick`/`StepBars`/`SeekToBar`/`SeekToTime`/`SnapCursorToNearestBar`, `BarRangeForSession`, `SimulatedAccount`/`SimulatedPosition`/`SimulatedFill` + `Equity`/`UnrealizedPnL`/`ApplyFill`/`Reset`, `SimulatedOrderBook` + `WorkingOrder` (trail state, stopTriggered flag), `EvaluateBar` (all 13 OrderTypes, outsideRth, TIF DAY cancel), `EvaluateTick` (hybrid TRADES/BID_ASK/MIDPOINT path). `tests/test_replay.cpp` — 207 cases / 987 assertions under `[replay]` tag covering all order types, clock, account math, tick evaluator, session boundaries.

- [x] **Task #69** — IBKRClient historical tick hooks (Task B) — `MsgHistoricalTick` variant with `std::vector<core::HistoricalTick>`, `onHistoricalTicks` callback, `ReqHistoricalTicks(symbol, whatToShow, ...)` method, 3 EWrapper overrides converting IB-native `HistoricalTick`/`HistoricalTickBidAsk`/`HistoricalTickLast` → `core::HistoricalTick` batches, dispatch in `ProcessMessages`. 3 new dispatch tests in `test_ibkr_queue.cpp` (226 total).

- [x] **Task #70** — ReplayWindow shell + main.cpp wiring (Task C) — `src/ui/windows/ReplayWindow.{h,cpp}` (~670 lines): toolbar (symbol/date/session/TF/Load/playback/speed/mode/equity/Reset + PAPER badge), ImPlot candlestick chart with cursor line and fill markers, scrubber with time label, status bar, mode-switching (Analysis/Operate), order entry placeholder. `main.cpp`: `ReplayEntry` struct, `SpawnReplayWindow(idx)`, `CreateTradingWindows`/`DestroyTradingWindows` wiring, Windows → IBKR → Replay menu with "+ New Replay" button, `Render()` loop call. `CMakeLists.txt` updated.

- [x] **Task #71** — Engine wiring + order entry + group sync (Task D) — Per-frame `ApplyEngineTick()` advances clock and evaluates crossed bars via `EvaluateBar`. `OnCursorMove` callback fires `BroadcastReplayCursor()` with 100ms throttle per group. Order entry expanded to 13 types with conditional fields, confirmation popup. Equity input, tick-fills stub, working orders list with cancel buttons.

- [x] **Task #72** — Bottom tabs + AI placeholder + persistence (Tasks E/F/G) — Tab bar: Actual Trades (double-click to scrub), Simulated Orders (Operate mode only), Stats (real/sim PnL, equity, win rate), AI Analysis (Copy day summary to clipboard → Markdown, greyed-out provider buttons for Anthropic/OpenAI/DeepSeek). `replay-windows.cfg` persistence with atomic `.tmp`+`rename`, per-second flush, restore on `FinishConnect`.

- [x] (unplanned, 2026-05-04) — **ReplayWindow sandbox trading port** — replaced the combo-form `DrawOrderEntry` with a ChartWindow-style trade UI: inline `DrawTradePanel()` (Qty + 13-type combo + per-type fields ($/% trail toggle, lmt offset, peg offset) + DAY/GTC TIF + green/red BUY / SELL buttons + Transmit-Instantly checkbox), `DrawPositionStrip()` (qty / entry / last / unreal P&L from `m_account.positions[m_symbol]`; uses cursor-bar close as last-price), `DrawOrderImpactBadge()` (mirrors `core::services::ComputeOrderImpact` against the simulated account — OPEN/ADD/REDUCE/CLOSE/FLIP coloured strip), `DrawWorkingOrders()` (dashed h-lines on the chart for every entry in `m_book` with side-coloured side-tag — STP+LMT legs for StopLimit/LIT, Trail uses live `wo.trailStop`), `DrawArmedLineAndHandleClick()` (chart-click arming for needsPrice types; single-price → fire on click; dual-price StopLimit/LIT → first click locks trigger/stop, second click sets limit; Esc disarms; Ctrl+click forces confirm even when Transmit-Instantly is on), and `DrawConfirmPopup()` (modal viewport-centred, full per-type field readout, PAPER badge, Confirm/Cancel/Esc). New `m_orderQty` / `m_orderTypeIdx` / `m_tifIdx` / `m_transmitInstantly` / trail+peg knobs / `m_limitArmed` / `m_limitSide` / `m_firstPricePlaced` / `m_firstPrice` / `m_liveCursorPrice` / `m_pendingConfirmOrder` / `m_showConfirmPopup` members on `ReplayWindow`. Render order: toolbar → scrubber → unguarded strip → (Operate-only) trade panel → (Operate-only) position strip → chart (with working-order overlay + armed-line click-handler inside `BeginPlot`/`EndPlot`) → status bar → bottom tabs → confirm popup. Submit path: every fire path stages a `core::Order`, calls `OnPaperOrderSubmit(o)` → returns localId, then pushes `core::services::WorkingOrder { localId, order, placedAt = m_xs[cursor] }` into `m_book` so `EvaluateBar` (per-frame in `ApplyEngineTick`) can fill it as the clock crosses each bar. File-local `kOrderTypes[]` table mirrors ChartWindow's; file-local `DrawDashedHLine` helper avoids exposing the ChartWindow static. `outsideRth` is forced ON for replay (the bar evaluator already filters by `BarSession`, and replay covers all sessions). Build clean, 226/226 tests pass; live IB smoke deferred (manual).

- [x] **Task #73** — Documentation (Task H) — This file. The main GLFW window is borderless (`GLFW_DECORATED=GLFW_FALSE`) so the app implements its own edge-resize via `glfwGetCursorPos(g_AppWindow, ...)` + edge-zone math. With `ImGuiConfigFlags_ViewportsEnable` on, an undocked window becomes a separate OS-level GLFW window that can sit over (or past) the main window's borders. `glfwGetCursorPos` still returns coordinates relative to the main window in that case, so `mx > ww - kEdge` / `mx < kEdge` / `my > wh - kEdge` would all evaluate true when the cursor is on a floating viewport at the edge — and `glfwSetWindowPos/Size(g_AppWindow, ...)` would resize the main window every frame the user dragged the floating window. Fix: gate the edge-detection block behind `glfwGetWindowAttrib(g_AppWindow, GLFW_HOVERED)` (false when another window is between the cursor and the main window's content), with an `s_resizing` exemption so a resize started on a real main-window edge can drag past the edge without snapping back. The static drag-state declarations (`s_resizing`, `s_edge`, `s_startMX/MY/WX/WY/WW/WH`) were hoisted to the top of the function so the early-return can consult `s_resizing` before any of the other math runs; the cursor is reset to `nullptr` on the early-return path so a stale resize cursor doesn't linger when the cursor re-enters the main window.

## Phase 15: Order Flow Insights — ChartWindow

Plan at `.claude/plans/order-flow-insights.md`. Three additions on top of the auto-analysis layer to help the trader read **why** price is moving — Volume Profile (CW-1), CVD sub-pane (CW-3), and price/CVD divergence detector (CW-4). ChartWindow only this phase; TradingWindow widgets and footprint chart deferred.

- [x] (unplanned, 2026-05-06) — **Bracket order TP leg + OCA pairing + live STP/TP P&L preview** — extended ChartWindow's existing two-click Bracket (entry LMT + stop-loss STP) into a true IB-style three-leg bracket (entry + STP stop-loss + TP take-profit, OCA-linked) with click order **Entry → STP → TP** so the cursor bubbles for the STP and TP placement phases can show live projected dollar loss / gain, percentage move from the locked entry, and live R:R as the user positions each leg. `core::Order` gains `std::string ocaGroup` and `int ocaType` fields; `IBKRClient::PlaceOrder` propagates both to IB's `::Order` when `ocaGroup` is non-empty (defaulting `ocaType` to 1 = cancel-with-block). `ChartWindow::PendingBracketStop` extended with `double tpPrice = 0.0` (0 = legacy stop-only bracket — backward-compatible); new `m_secondPrice` / `m_secondPricePlaced` / `m_bracketTpPrice` members.

`drawArmedLine` extended with optional `pct` (NaN default) and `extra` (nullptr default — free-form trailing string like `"R:R 2.34"`) parameters; bubble + right-edge label share the same composer so format is consistent. New per-leg helpers `bracketLegPnL(price, entry)` / `bracketLegPct(price, entry)` (sign-aware so longs and shorts both render losses negative / gains positive).

The dual-price click handler split into:
  - **Phase 1 (Entry)**: cursor draws orange ENTRY line with cost label; click stores `m_firstPrice = cursorPrice`.
  - **Phase 2 (STP)**: locked orange ENTRY (with cost label) + red/blue STOP cursor showing ` STOP $X  -$Y (-Z%) `. BUY rejects STP at-or-above entry; SELL mirrors. Click stores `m_secondPrice = cursorPrice` (does NOT fire).
  - **Phase 3 (TP)**: locked ENTRY + locked STP (with frozen ` STOP $X  -$Y (-Z%) ` label) + green TP cursor showing ` TP $X  +$Y (+Z%)  R:R W `. BUY rejects TP at-or-below entry; SELL mirrors. Click fires the entry LMT and stuffs `{symbol, stopSide=opposite, qty, stopPrice=m_secondPrice, tpPrice=cursor}` into `g_pendingBracketStops`.
  - Non-bracket dual-price (StopLimit / LIT) keeps the existing two-click fire behaviour.

`DrawConfirmPopup()` renders all three legs with computed R:R when `m_isBracketConfirm`; type display reads `"BRACKET (LMT entry + STP + TP)"`. Toolbar hint cycles through `"Click chart to set ENTRY (LMT)"` → `"... STOP-LOSS"` → `"... TAKE-PROFIT"`. `DrawOrderImpactBadge()` uses `m_firstPrice` (locked entry) as the fill-price proxy from phase 2 onward so the position-impact strip reflects the open-position impact, not a future closing leg.

`main.cpp` `onFillReceived` for bracket entry submits STP and (when `tpPrice > 0`) TP as a pair sharing `ocaGroup = "BRK_<entryId>"` and `ocaType = 1` — IB cancels the survivor when one fills. All armed-state reset sites (`SetSymbol`, type-combo change, `fireOrder` re-arm, `Use suggestion`, Esc in trade panel, popup Confirm/Cancel/Esc) clear `m_secondPrice` / `m_secondPricePlaced` / `m_bracketTpPrice` alongside the existing first-price reset. Pending-order overlay format prints `BUY BRK ENTRY $... STP $... TP $...` once phase 3 is reached, or `BUY BRK ENTRY $... STP $...` mid-phase-2. Build clean, 288/288 ctest tests pass; live IB smoke-test deferred (manual).

- [x] (unplanned, 2026-05-06) — **Stale-tick contamination fix on chart symbol change** — `main.cpp` `ReqChartData` was cancelling the old market-data subscription and immediately re-issuing on the *same* `mktId` for the new symbol. IB keeps streaming ticks for a few ms after `CancelMarketData`, so a stale e.g. TSLA HIGH at 398.55 would arrive after `g_tickerSymbols[mktId]` had already been overwritten to the new symbol — passing the dispatcher's `tickerId == ce.mktId && sym == ce.win->getSymbol()` validation and corrupting OHLC on the new chart (visible as a candle high far above the symbol's actual range). Fix: rotating mktId pool. Added `inline int AllocChartMktId()` (1000-slot pool 10000–10999, wraps). `ChartEntry::mktId` is now allocated from `ChartMktId(idx)` (100-109) only as the initial placeholder; `ReqChartData` is refactored to take `ChartEntry&` by reference, and on every symbol change it calls `CancelMarketData(ce.mktId)` → `g_tickerSymbols.erase(ce.mktId)` → `ce.mktId = AllocChartMktId()` → `g_tickerSymbols[ce.mktId] = sym` → `ReqMarketData(ce.mktId, sym, ...)`. Stale ticks from the old subscription land with the previous reqId, fail the `tickerId == ce.mktId` check at the dispatcher (lines 2162/2189/2195/2201), and are silently dropped. All four call sites updated: `OnDataRequest` lambda (line 952), Scanner `OnSymbolSelected` chart-broadcast loop (line 1109), chart-modes restore (line 1830), and `DrainStyleSwitchQueue` (line 933). `DestroyTradingWindows()` cleanup also updated to `g_tickerSymbols.erase(e.mktId)` on each entry before clearing the vectors (the old static-range loop only knew about ChartMktId(i) initial slots and would leak rotated entries). Build clean, 288/288 ctest tests pass; live IB smoke-test deferred (manual). The same cancel→update→resubscribe pattern exists on TradingWindow paths (lines 808-812, 1117-1120) and is theoretically vulnerable to the same race; not changed in this fix since the user reported only the chart symptom.

- [x] (unplanned, 2026-05-06) — **Bracket modify-in-place fix for OCA pairing** — When the user dragged a STP or TP leg of a live bracket order to a new price, the chart's `OnModifyOrder` callback (main.cpp:1042) was doing **cancel + re-place with a new `orderId`**. Both legs were submitted in `onFillReceived` with `ocaGroup = "BRK_<entryId>"` and `ocaType = 1` (cancel-with-block); cancelling one OCA member breaks the pairing and IB cancels the survivor (or refuses to re-pair the new leg since OCA group membership is locked at submission time). User reported: "if I move STP, it cancels TP". Fix: switch the lambda to **modify in place** — re-issue `PlaceOrder` with the *same* `orderId` (IB API treats `placeOrder` with an existing orderId as an order modification). The OCA pairing remains intact, neither sibling gets cancelled. `g_liveOrders[orderId]` is updated in place (no erase + reinsert with new id), `g_nextOrderId` is no longer bumped, `SetNextOrderId` broadcast on trading windows is removed (no new orderId allocated), and `status`/`submittedAt`/`filledQty`/`avgFillPrice` are no longer reset (those are server-tracked and would mis-render the order's actual state otherwise). Per-type field updates unchanged (Limit→limitPrice, Stop→stopPrice, StopLimit→both). **Follow-up**: initial in-place modify rejected by IB with error 10327 ("OCA group type revision is not allowed"). First attempted fix (strip `ocaGroup`/`ocaType` from the wire copy before `PlaceOrder`) **did not resolve it** — 10327 fires whenever the resent OCA membership *differs from* IB's server-side state, and dropping the fields counts as "going from member → non-member" = revision. **Actual root cause**: `IBKRClient::openOrder` (services/IBKRClient.cpp:966) was not copying `ocaGroup` / `ocaType` / `parentId` / `account` / `exchange` / `transmit` from IB's `::Order` into `core::Order`. The `onOpenOrder` handler in main.cpp (line 2371) does `g_liveOrders[order.orderId] = order;`, so every IB ack of our bracket STP/TP **clobbered** the in-memory OCA fields back to defaults (`ocaGroup=""`, `ocaType=0`, `transmit=true`). When the user dragged a leg, `OnModifyOrder` then resent `ocaGroup=""` while IB still had `BRK_<entryId>` server-side → 10327. Real fix: extend `IBKRClient::openOrder` to populate all six fields from IB's `::Order` (and `c.exchange` from the contract) so `g_liveOrders` mirrors server state. With that in place, the modify resends matching values and IB accepts the price update without breaking the OCA pairing. Wire-strip in `OnModifyOrder` reverted — `PlaceOrder(rep)` now sends the full order including OCA fields. Build clean.

- [x] (unplanned, 2026-05-07) — **ChartWindow defense-in-depth on symbol switch** (follow-up to histId rotation, same user report). Three additions in `src/ui/windows/ChartWindow.cpp`: (1) `SetHistoricalData` drops bars whose `series.symbol` doesn't match `m_symbol` — backstops the histId rotation against any other path that could deliver stale bars from a previous symbol; (2) `RequestNewData` now fully clears every indicator/auto-analysis array (`m_idxs`, `m_sma1/2`, `m_ema`, `m_bb*`, `m_rsi`, `m_atr14`, `m_autoSupports/Resistances`, `m_autoTrend`, `m_donchHi/Lo`, `m_keltUpper/Lower`, `m_breakouts`, `m_setup`, `m_breakoutSignal`, `m_breakoutLevelIdx`) and resets `m_priceMin/m_priceMax` to 0 — prevents a render between `RequestNewData` and the next `ComputeIndicators` from plotting stale values from the previous symbol or fitting a stale Y-axis range; (3) `InitViewRange` skips zero/sentinel OHLC values when computing `pMin/pMax` and falls back to a ±1% range around the last close (or 1.0) when every bar in the window has zero OHLC — prevents the linked `[0, 0]` Y-axis collapse where SMA/VWAP plot as a single horizontal line while candles/volume/RSI render invisible because every value falls on the same y-coordinate. Build clean, 288/288 ctest tests pass.

- [x] (unplanned, 2026-05-07) — **Stale-historical-bar contamination fix on chart symbol/TF change** (futures group regression). User report: "I open 3 chart groups with normal stock and then I add the future over those 3 — at some moment I can't see the candles, maybe they're shrunk." Root cause: the stale-data race fixed for market-data ticks via `AllocChartMktId()` rotation (entry above) was **also present on the historical-bar path** (`ce.histId`) and the pan-left extend path (`ce.extId`), which were never rotated. When a chart switches from AAPL (~$200) to /ES (~$5500), `ReqChartData` cancels and immediately re-issues `ReqHistoricalData` on the same `ce.histId`. IB keeps streaming bars for a few ms after `CancelHistoricalData`, so stale AAPL bars arrive on `histId` after `pendingBars` has been cleared and re-tagged for `/ES`, get appended, and `SetHistoricalData` accepts a corrupted mix. `InitViewRange` then computes `pMin/pMax` over the last 100 bars (which include both AAPL ~$200 and /ES ~$5500 entries), fits the Y-axis from $200 to $5500, and every candle renders as a 1-pixel sliver — visually "candles disappear / shrunk". With three charts in a group all switching simultaneously the symptom is more reliable since IB's response interleave is wider. Fix mirrors the established mktId pattern: added `inline int AllocChartHistId()` (1000-slot pool 12000–12999, wraps) and `inline int AllocChartExtId()` (1000-slot pool 13000–13999). `ReqChartData` now rotates `ce.histId` and `ce.extId` on every cancel→reissue cycle (not gated on `symbolChanged` — TF changes via `DrainStyleSwitchQueue` produce the same race for the same-symbol-different-TF case). `ce.pendingExtBars` is also reset in the same block so a previously in-flight extend doesn't merge stale bars from an earlier symbol/TF into the new prepend. Static-range `ChartHistId(idx)` / `ChartExtId(idx)` (1-19 / 2-20) remain as initial slots only — the dispatcher (lines 2002, 2012) routes by `reqId == ce.histId`/`ce.extId` so rotated ids work without dispatcher changes; stale bars on the old id match no chart entry and are silently dropped by virtue of the for-loop fall-through. Build clean, 288/288 ctest tests pass; live IB smoke-test deferred (manual).

- [x] **Task #74** — Volume Profile primitive + ChartWindow integration (Task A of the plan, 2026-05-06) — `core::services::ChartAnalysis.h` gains `VolumeBin` / `VolumeProfile` structs and `ComputeVolumeProfile(highs, lows, volumes, priceLo, priceHi, numBins=50) → VolumeProfile{bins, pocIdx, maxVolume, totalVol, valueAreaLoIdx, valueAreaHiIdx}` as inline free function. Each bar's volume is distributed *uniformly across the bar's [low, high] range*, normalised against the bar's *full* range (so a partially-clipped bar contributes only `visibleRng / fullRng` of its volume) — this matches TradingView / IB convention and avoids the "POC sits at one big bar's close" artefact of pure-typical-price profiles. POC = `argmax(bins.volume)`. Value Area (~70% of total volume) computed by expanding outward from POC, picking whichever side has the larger neighbouring bin (right-bias on ties). `numBins` is clamped to `[10, 200]`; degenerate inputs (`priceHi <= priceLo`, mismatched lengths, empty bars) yield empty profile / `pocIdx = -1`. `tests/test_chart_analysis.cpp` adds 12 cases / 72 assertions under the `[volume-profile]` tag covering single-bar uniform distribution, POC concentration, out-of-range bars (above + below), partial overlap proportional contribution, empty input, degenerate range, mismatched length, `numBins` clamping, zero-volume bars skipped, value-area expansion (synthetic profile [10,20,50,20,10] → POC=4, VA=[3,5]), and the clamped `numBins=4` path produces a valid value area. `ChartWindow.h` adds `IndicatorSettings::volumeProfile = false` + `vpBins = 50`, `using VolumeProfile = core::services::VolumeProfile`, member `m_vp` (re-bucketed every frame from `ImPlot::GetPlotLimits()` Y-range), and `DrawVolumeProfile()` decl. `ChartWindow.cpp` `DrawVolumeProfile()` is invoked from `DrawCandleChart()` between candlesticks and overlays, gated on `m_ind.volumeProfile`: it fetches the visible Y-range, calls `ComputeVolumeProfile`, then draws translucent right-anchored horizontal bars via the plot drawlist (not `PlotBarsH`, so it doesn't interact with the X-axis or auto-fit). Histogram occupies the rightmost ~25% of the plot at the POC; bins scale proportionally against `m_vp.maxVolume`. Three alphas: ~18% normal, ~29% value-area, ~43% POC + thin gold POC border. `DrawAnalysisToolbar()` Auto: row gains `VP` checkbox after `Setup`. `DrawAutoSettingsPopup()` gains a Volume Profile section with `VP bins` `InputInt` (10–200). `core::services::TradingStyle.h` `StylePreset` gains `indVolumeProfile` field; per-mode defaults: Scalping=false (visual noise on M1), Day Trading=true, Swing=true, Investment=false (weekly bars don't need it), Free=false (construction baseline). `ApplyPreset(...)` stamps the field onto the caller's `IndicatorSettings`. `[style]` test cases extended on all 5 presets + the ApplyPreset round-trip case + the Free baseline case (verifies a DayTrading→Free round-trip clears `volumeProfile` back to `false`). 287 cases / 1322 assertions pass; build clean, all 275 ctest tests pass. Live IB smoke-test deferred (manual).

- [x] (unplanned, 2026-05-08) — **Stale-bar contamination: stream-active gates + histId/extId rotation completion** — Completed the Phase 15 stale-data fixes with two additions: (1) `DrainStyleSwitchQueue` now sets `ce.extStreamActive = false` alongside the extId cancel, closing a micro-window where stale extend completions could leak through; (2) reconnect path in `FinishConnect` now clears `ce.pendingBars.bars`, cancels in-flight extId, and resets `ce.extStreamActive`/`ce.pendingExtBars` before re-subscribing — prevents pre-disconnect bars from merging into reconnected data.

- [x] (unplanned, 2026-05-08) — **`ShouldReplaceHistoricalBars` ratchet + tests** — Extracted the `SetHistoricalData` rejection logic into `core::services::ShouldReplaceHistoricalBars(existingCount, existingSymbol, newSymbol, newCount) → bool` in `ChartAnalysis.h`. Three guards: empty completion on existing data → reject; symbol mismatch (except for fresh charts with empty existing symbol) → reject; data-loss ratchet (existing ≥50, new ≤5) → reject. Wired into `ChartWindow::SetHistoricalData`. `tests/test_chart_analysis.cpp` adds 6 cases / 16 assertions.

- [x] (unplanned, 2026-05-08) — **`PendingBracketStop` moved to `OrderData.h` + default-value test** — Struct moved from `ChartWindow.h` to `core/models/OrderData.h` so it can be unit-tested. `ChartWindow.h` now aliases `using PendingBracketStop = core::PendingBracketStop;`. `tests/test_models.cpp` adds `[bracket][defaults]` test covering all 7 fields.

- [x] (unplanned, 2026-05-08) — **Futures market health BID/ASK fix** — `ChartWindow::OnFuturesTick` was treating BID (field 1) and ASK (field 2) as equivalent to LAST (field 4) for the displayed price. For /NQ with a ~10-point spread, the values oscillated between bid/ask/last, showing different prices on different charts. Fix: only LAST (field 4) updates the displayed price. `DrawFuturesItem` now shows price-only when prevClose hasn't arrived (avoids bogus `+29158.75 (0.00%)`).

- [x] (unplanned, 2026-05-08) — **Group system: 4→10 groups + out-of-bounds crash fix** — `kNumGroups` raised from 4 to 10 in `WindowGroup.h`, matching `kMaxMultiWin`. `Spawn*` functions wrap with `(idx % kNumGroups) + 1`. `BroadcastGroupSymbol` upper-bound check added. GDB backtrace revealed segfault at `BroadcastGroupSymbol(groupId=5)` accessing `g_groups[4]` past end of 4-element array, corrupting `g_selectedAccount`'s string buffer. `DrawGroupPicker` popup now dynamically shows G1–G10. TWS display group sync still guards `groupId <= 4`.

- [x] (unplanned, 2026-05-08) — **Bracket after-hours guard** — During extended hours, IB does not trigger STOP orders. Bracket submission now detects session via `BarSession(std::time(nullptr))`. Outside RTH: confirmation popup shows orange warning; stop type auto-switches from STP to STP LMT; limit price computed with 0.1% offset rounded to penny grid via `RoundToTick` (prevents IB error 110); `outsideRth=false` on stop leg, `true` on TP leg. During RTH, behavior unchanged. `core::PendingBracketStop` extended with `outsideRth`/`useStopLmt` bool fields.

- [x] (unplanned, 2026-05-08) — **TradingWindow DOM: click-to-trade on price, last-price highlight, legend** — Click-to-trade moved from full-row `MouseClicked[0]` to proper `InvisibleButton` overlaid on price column with `PushID(rowSeq++)/PopID()` (fixes ID conflict with multi-venue L2). `m_lastPrice` updated from `OnTick`/`OnTickByTick`, DOM row within ±$0.005 renders in gold with `*` suffix. Ladder legend (green=Bid depth, red=Ask depth, blue=Executed volume) added below submit button. `OnFuturesTick` no longer accepts BID/ASK for price display.

- [x] (unplanned, 2026-05-08) — **Chart sub-plot splitters + legend toggle** — Two draggable splitter bars (TradingWindow-style: grey→white-grey `AddRectFilled`, ↕ cursor, `ImGuiButtonFlags_MouseButtonLeft`, 5px, std::clamp) between Price↔Volume (adjusts `m_volumeHeightRatio` 5%–50%) and Volume↔RSI (adjusts `m_rsiHeightRatio` 5%–40%). Heights pre-computed in `DrawCandleChart()` and stored in `m_cachedVolumeH`/`m_cachedRsiH`. "Legend" checkbox in toolbar toggles `ImPlotFlags_NoLegend` on the main price chart.

- [x] (unplanned, 2026-05-09) — **Custom title bar made non-dockable** — `RenderCustomTitleBar()`'s `##titlebar` `Begin()` call was missing `ImGuiWindowFlags_NoDocking`, so the IBKR TRADING TERMINAL header at the top of the main window could be picked up by ImGui's drag-to-dock logic and docked into the dockspace beneath it. Added the flag alongside the existing `NoDecoration | NoMove | NoScrollbar | NoSavedSettings | NoBringToFrontOnFocus | NoNav` set. Build clean.

- [x] (unplanned, 2026-05-09) — **Edge-resize spurious start during docking drag** — Follow-up to the multi-viewport docking fix below. When a floating viewport was dragged toward a screen edge, the OS/window manager clamps the floating window at the edge while the cursor keeps moving — the cursor then exits the floating viewport and lands on the main app's edge zone. `HandleWindowResize` (`main.cpp:3383`) was guarding the start-of-resize on `IsMouseDown(Left)`, which is true throughout a drag, not only on the click frame. So the function would interpret "LMB still held + cursor now in edge zone" as a fresh resize gesture, set `s_resizing=true`, and start calling `glfwSetWindowPos`/`glfwSetWindowSize` on the main window every frame — the dockspace then shrinks/grows under the cursor, the drop-target rectangles slide out from where the user is aiming, and the window is hard to drop. **Fix**: split the LMB read into `lmb = IsMouseDown` (used for the on-release reset of `s_resizing`/`s_edge`) and `lmbClicked = IsMouseClicked` (used as the start-resize gate). A new resize can only kick off on the click frame, never mid-drag from an unrelated LMB hold. Existing edge-resize behaviour is preserved (clicking on an edge still works) because `IsMouseClicked` fires on that frame; subsequent frames keep resizing through the `s_resizing=true` path. Build clean.

- [x] (unplanned, 2026-05-11) — **Bracket STP/STP LMT outsideRth fix + IB hold-reason plumbing** — User report: after-hours bracket placed via ChartWindow showed the STP LMT leg in IB's Open Orders panel but the stop never triggered during pre-market price moves. Two coupled root causes addressed:

  1. **Bracket stop leg honoured `outsideRth=false` even when the bracket itself was placed outside RTH** (`main.cpp` `onFillReceived` bracket handler, line 2719). The original code hardcoded `stop.outsideRth = false` with the comment "stop condition only triggers during RTH regardless" — this was wrong. For stocks with extended-hours trading IB *does* evaluate stop triggers in pre/after-market, but only when the flag is set on the order. With it false IB parked the STP LMT until the next RTH open, leaving the position unguarded for the entire ext-hours session. **Fix**: changed to `stop.outsideRth = p.outsideRth;` — `PendingBracketStop.outsideRth` is already set to `true` outside RTH and `false` during RTH by both bracket fire sites in `ChartWindow.cpp` (popup confirm path line 2423, transmit-instantly path line 3232). The TP leg already used `p.outsideRth` (line 2740) so this only required the stop-leg flip.

  2. **No UI surface for IB's "this order is being held" warnings.** IB emits `error()` callbacks with informational codes (399 = order message warning, 404 = held until market open, 2109 = outside-RTH attribute ignored, 2148 = order held: routed but not yet eligible, 10311 = order has constraint but not rejected) when an order is accepted but parked. Previously these arrived at `main.cpp`'s `onError` lambda and either got swallowed (no order found) or — for known orderIds — were treated as rejections (`status = Rejected` + `rejectReason = msg`), which was visibly wrong because the order was still live in IB's blotter. **Fix**: added `core::Order::holdReason` (informational warning text; distinct from `rejectReason`) and `core::services::PendingOrderLine::holdReason`. Routed the five hold-warning codes through a new branch at the top of `onError` that stamps `g_liveOrders[id].holdReason`, broadcasts to `OrdersWindow::OnOpenOrder` (which now merges non-empty `holdReason` into existing records — see `OrdersWindow.cpp:OnOpenOrder` change), calls `UpdateAllChartPendingOrders()` so the chart overlay picks it up, fires a Warning-severity notification, and `return`s before the rejection path runs. `PendingOrderLine` propagation added in `main.cpp::UpdateChartPendingOrders`.

  UI surface:
   - **OrdersWindow** status column (`DrawOrderRow` col 13): when `!holdReason.empty() && !IsTerminal(status)`, appends a yellow `HELD` chip after the status text with a hover tooltip showing the full `[code] message`. Cleared automatically once the order transitions to Filled/Cancelled/Rejected.
   - **ChartWindow pending-order overlay** (`drawOrderLeg`): held orders override the leg's normal side colour (blue for BUY, red for SELL) and the aux-leg orange with an amber palette (line `IM_COL32(220,170,40,210)`, label bg `IM_COL32(90,60,0,255)`); the row label gets a ` ⚠ HELD ` suffix on the main leg only (aux is the limit twin of the same order and would just duplicate the tag). The suffix is appended once at the end of the label-build pipeline so it survives the existing dragging / P&L rebuilds.
   - **TradingWindow** blotter intentionally not wired — its `m_openOrders` only tracks locally-submitted orders, so the bracket case (where stop/TP legs are submitted by `main.cpp` on entry fill) wouldn't surface there anyway. OrdersWindow is the global view.

  `tests/test_models.cpp` already asserts the new `Order::holdReason` defaults (covered by the existing `o.rejectReason.empty()` assertion sibling — no test edit needed since `holdReason` is just another `std::string` defaulting to empty). Build clean; 311/311 ctest tests pass. Live IB smoke-test deferred (manual): place an after-hours bracket on a liquid stock and confirm (a) STP LMT carries `outsideRth=true` in IB's API logs, (b) ext-hours triggers fire when price crosses the stop, (c) when IB still holds the order (futures or non-eligible stock), the OrdersWindow row shows a yellow `HELD` chip and the chart leg goes amber with `⚠ HELD`.

  **Follow-up (same session): notifications for Working / Working-Held transitions.** Per user request the toast/sound stack now fires when an order goes live, not just when it fills, rejects, or cancels. Added two events to `core::services::NotificationEvent` (appended after `Test` to preserve existing integer values for any persisted state): `OrderWorking` and `OrderHeld`. Matching `kEventMeta` rows added in `NotificationService.cpp` (`"order_working"` / `"order_held"`). `tools/gen_tones.cpp` extended with two new tone generators (`buildOrderWorking` — short sine ping at 720 Hz, neutral; `buildOrderHeld` — two-tone descending 540→420 Hz, "warning-but-not-error" cadence). In `main.cpp::onOrderStatusChanged` the lambda now snapshots the previous `status` from `g_liveOrders` before overwriting it; when the transition is **Pending → Working** AND `g_liveOrders[orderId]` exists, fires `OrderWorking` (Info severity, body `"<sym> <type> <qty> @ live"`) — or `OrderHeld` (Warning severity, body includes `holdReason`) when `holdReason` is already populated. Dedupe-by-prev-state means repeat `Submitted`/`PreSubmitted` updates (price modifies, OCA group changes, partial-fill re-emit) don't re-toast. The `onError` hold-warning path was also switched from emitting `IbError` to `OrderHeld` so the toast event matches what the row chip shows and the per-event mute/sound knobs line up. Missing WAVs degrade gracefully (`ma_engine_play_sound` returns non-success and the audio backend just logs to stderr); regenerate via `./build/tools/gen_tones --force assets/sounds/tones/`. Build clean; 311/311 tests pass.

  **Follow-up (same session): duplicate "IB error 202" toast on cancel.** User report: every cancel raised a second Error-severity toast titled `"IB error 202"` with body `"Order Canceled - Reason: -"` on top of the expected `"Order cancelled"` Warning toast. Root cause: IB emits both `orderStatus(Cancelled)` AND `error(code=202, "Order Canceled - Reason: ...")` for every cancel. The orderStatus path already fires `OrderCancelled` (with `g_userCancelled` dedup so a user-initiated cancel stays quiet); the curated IB-error allowlist in `onError` then also surfaced code 202 as a generic `IbError` toast — that's the duplicate. The `alreadyRejected` guard only checked `OrderStatus::Rejected`, not `Cancelled`. Fix in `main.cpp::onError`: (a) removed `code == 202` from the surface allowlist with a comment explaining why (the orderStatus path is the authoritative cancel notification, and 202 carries no extra info); (b) widened the dedup guard from `alreadyRejected` to `alreadyHandled` covering both `Rejected` and `Cancelled` terminal states, so any remaining allowlist code (110/201/321/354/10148/10149) that arrives after a cancel/reject also stays quiet. The 10148/10149 codes are still filtered upstream in `IBKRClient::error` and shouldn't reach this path, but the guard is now correct if they ever did. Build clean; 311/311 tests pass.

  **Follow-up (same session): user-initiated cancels were silent.** After dropping code 202 from the IbError surface, the user reported the `OrderCancelled` toast wasn't firing on user-initiated cancels — they only saw it before because the (now-removed) `IB error 202` toast happened to fire. Root cause: the existing `onOrderStatusChanged` cancel-toast block had a `g_userCancelled` dedup set that deliberately suppressed the toast when the user clicked Cancel themselves (assumption was "user knows they cancelled, no need to toast"). Combined with the 202 removal that left every user-initiated cancel completely silent. Fix: removed the `g_userCancelled` dedup entirely. Three cancel-button handlers in `main.cpp` (ChartWindow's `OnCancelOrder` at line ~1146, TradingWindow's `OnOrderCancel` at line ~1205, OrdersWindow's `OnCancelOrder` at line ~1870) no longer `.insert(orderId)`; the static declaration at line 147 is gone; the suppression check in the cancel-toast block is gone. Every `orderStatus(Cancelled)` now fires `OrderCancelled` (Warning severity, body `"<sym> <type>"`) regardless of origin — the toast acts as confirmation that IB actually processed the cancel request, which is what the user wants. Build clean; 311/311 tests pass.

  **Follow-up (same session): OrderWorking / OrderHeld toasts never fired.** User report: tone, voice, and toast for the new working/held events weren't firing on any order placement. Root cause: the transition check in `onOrderStatusChanged` was `prevStatus == Pending && status == Working`, but IB *always* fires `openOrder` (status `Submitted` → parsed as `Working`) **before** `orderStatusChanged`. The main.cpp `onOpenOrder` lambda did `g_liveOrders[order.orderId] = order;` which clobbered the locally-set `Pending` to `Working`, so by the time `onOrderStatusChanged` snapshot ran, `prevStatus` was already `Working` and the transition check could never match. Secondary failure mode: on connect, `reqAllOpenOrders` fans out `onOpenOrder` for every existing IB order — even if we'd fired on `onOpenOrder` directly, we'd toast every pre-existing order every reconnect.

  **Fix**: replaced the prev-status check with a `g_pendingLocalAccept` set tracking orderIds that we've just placed locally and haven't yet observed accepted at IB. Set declared at line ~148 (placement chosen so it sits next to `g_NotificationService`). New static helper `MaybeNotifyOrderAccepted(int orderId)` fires the `OrderWorking` (Info) or `OrderHeld` (Warning, when `holdReason` already populated) toast the first time the order is observed in `Working` or `PartialFill` status; idempotent via the set membership check + erase-on-fire. Wired into both `onOpenOrder` (so we don't miss IB's first ack) and `onOrderStatusChanged` (so we still toast if `orderStatus` somehow arrives before `openOrder`). Four order-submission sites now insert into the set right before `PlaceOrder()`: ChartWindow `OnOrderSubmit` (`main.cpp` line ~1144), TradingWindow `OnOrderSubmit` (line ~1200), bracket STP leg in `onFillReceived` (line ~2805), bracket TP leg (line ~2826). `onOrderStatusChanged` also erases the id from the set on terminal status (Filled / Cancelled / Rejected) so an instantly-rejected order doesn't leak the slot. `OnModifyOrder` does NOT insert (it's a modification of an existing live order, not a new submission). On connect, `g_pendingLocalAccept` is empty, so the `onOpenOrder` fan-out from `reqAllOpenOrders` correctly toasts nothing — pre-existing orders stay quiet. If IB sends `error(code=404, "held until open")` between `openOrder` and `orderStatusChanged`, `holdReason` is set first → `MaybeNotifyOrderAccepted` then fires `OrderHeld` instead of `OrderWorking`. If the error arrives *after* the accept toast (rare but possible), the held-side error path in `onError` still fires its own `OrderHeld` Warning toast, so the user always sees the held state surface at least once. Build clean; 311/311 tests pass.

  **Follow-up (same session): tone + voice WAVs missed by initial fix.** User reported toast now firing but tone and voice silent. Root cause: the `Notify()` call was correct, but the asset files referenced by the new events (`assets/sounds/tones/order_working.wav` / `order_held.wav` and the matching `voice/` paths) didn't exist on disk. miniaudio's `ma_engine_play_sound` returns non-success on missing files and just logs to stderr — so toast appeared but audio was silent. Generated all four WAVs: (a) re-enabled `IBKR_BUILD_TOOLS=ON`, built the updated `gen_tones` target (now 13 entries including `buildOrderWorking` / `buildOrderHeld`), ran it against `assets/sounds/tones/` — wrote the two missing files, skipped the 11 existing committed ones. (b) Added `order_working | Order working` and `order_held | Order held` to `tools/voice_phrases.txt` (single source of truth keyed off `EventName()`); ran `./tools/gen_voice.sh` (piper + sox pipeline) — wrote the two missing voice files. Both directories now contain 13 WAVs each (11 original + 2 new). `tools/gen_voice.sh` header comment updated to drop the hardcoded "11" count. WAV files are committed alongside the source change so other developers don't have to regenerate.

  **Follow-up (same session): ship sounds with the release artifact.** User report: "is CI prepared to ship voices and etc with the executable?" — diagnosis: no, three gaps. (a) All three platform jobs in `.github/workflows/build.yml` uploaded only `build/ibkr-trading-app` (or `.exe`), no assets dir — a downloaded artifact got a lone binary with no sound files. (b) `CMakeLists.txt` had `install(TARGETS ibkr-trading-app RUNTIME DESTINATION bin)` but no companion `install(DIRECTORY assets …)` — `cmake --install` wouldn't help either. (c) The runtime asset resolver in `main.cpp` used `/proc/self/exe` unconditionally, which only exists on Linux — on macOS / Windows the canonical-path call threw and the resolver fell through to `current_path() / "assets" / "sounds"`, breaking audio for any user who launched the binary from anywhere other than its own directory.

  Fixes:
  - **Cross-platform exe-dir lookup** in `main.cpp` notification-init block: `GetModuleFileNameW(nullptr, buf, 32768)` on Windows (32K-wide for long-path safety), two-call `_NSGetExecutablePath(nullptr, &bufSize)` size-query + alloc on macOS, `canonical("/proc/self/exe")` retained for Linux/BSD-with-procfs. Falls through to empty path on failure → resolver only tries CWD. Platform-conditional `#include <windows.h>` / `#include <mach-o/dyld.h>` added near the top of the include block.
  - **CMake install layout** at `CMakeLists.txt:342`: `RUNTIME DESTINATION` changed from `bin` to `.` so the binary sits at the install-prefix root, with `install(DIRECTORY assets DESTINATION .)` placing `assets/sounds/{tones,voice}/*.wav` as a sibling. The first resolver candidate (`<exeDir>/assets/sounds`) then hits directly — verified locally by `cmake --install build --prefix /tmp/ibkr-dist-test` followed by running the installed binary, which logged `[notify] assets: /tmp/ibkr-dist-test/assets/sounds` as expected.
  - **CI workflow** at `.github/workflows/build.yml`: added an `Install (binary + assets/) to dist/` step after `Run tests` for all three platform jobs (Windows uses `--config Release` because MSVC is a multi-config generator). The `upload-artifact` step changed from `path: build/ibkr-trading-app` (single file) to `path: dist/` (directory) for all three platforms. Users now download a single zip with the binary at the root and `assets/sounds/...` as a sibling — no setup or PATH/CWD adjustments needed for tone + voice playback to work out of the box. The `sanitize-linux` job is unchanged (doesn't ship).

  Docs: `.claude/rules/build.md` gains an "Install / Shipping Layout" section explaining the install rules + CI release pipeline; `.claude/rules/testing.md` CI Jobs table updated with the install/upload columns and a note about self-contained release artifacts. Build clean; 311/311 tests pass.

- [x] (unplanned, 2026-05-12) — **Overnight session shading visually distinct from Pre/After-Hours on ChartWindow** — `DrawSessionBands` (`ChartWindow.cpp`) had PreMarket `(40,80,140,38)` and Overnight `(80,40,140,38)` — both dark blue-purple at ~15% alpha — which blended into the same band on dark chart backgrounds, so users couldn't tell where the after-hours session ended and the overnight quiet period began. Hues moved to non-overlapping families: PreMarket → cleaner blue `(40,110,210,50)`, AfterHours → cleaner orange `(210,130,40,50)`, Overnight → dim charcoal-grey `(60,60,75,90)` with slightly higher alpha so the "lights out" band reads as a darker, neutral block instead of a muddy purple. Legend swatches updated to match (full alpha) so the legend dot for each session matches its band hue. Equity charts rarely contain overnight bars (IB filters them at the source unless ext-hours requested), but on futures (/ES, /NQ) where Globex runs through the night, the band now reads correctly. Build clean.

- [x] (unplanned, 2026-05-12) — **TradingWindow DOM S/R labels now respect live price** — On the order book, `R` / `S` tags came from `m_supportPrices` / `m_resistancePrices` which are computed against the chart's last close. Once the live tape moved through one of those levels, a row above the live price could still render as `S` (or vice versa), so the tags no longer matched the ladder's price ordering. Fix in `TradingWindow.cpp` `DrawOrderBook` `srTag` lambda: collapse the two-pass iteration into a single nearest-level search (smallest `|price - level|` within `tol`), then classify by position relative to a live `refPrice` (preferring `m_lastPrice`, then `m_midPrice`, then `(m_nbboBid + m_nbboAsk)/2`) — `level ≥ refPrice → "R"`, `level < refPrice → "S"`. When no live reference exists yet (fresh symbol switch before the first tick), falls back to the original "source list wins" behaviour. Build clean.

- [x] (unplanned, 2026-05-12) — **Futures chart candles disappear after pan-left history extend** — User report: "futures on chart window — data of the candles disappears suddenly and then continues printing again new candles." Root cause: `ChartWindow::PrependHistoricalData` (`ChartWindow.cpp:316`) shifts `m_xMin` / `m_xMax` right by the **raw** count of older bars returned by IB, but `RebuildFlatArrays()` (called immediately after the merge) then drops bars that fall in filtered sessions (Overnight when `m_showOvernight=false`; anything non-Regular when `m_useRTH=true`). For stocks during RTH the filter discards ~0 bars and the bug is invisible; for /ES intraday with `useRTH=false` + `showOvernight=false` the filter drops the entire 20:00-04:00 ET Overnight block (~8 hours / trading day). With several days of pan-left data that easily means hundreds of dropped bars. The over-shift pushed every previously-visible candle off-screen to the left ("candles disappear suddenly"); new live bars continued to land at the actual filtered tail but couldn't trigger `UpdateLiveBar`'s `newIdx >= m_xMax - 0.5` auto-scroll until enough of them accumulated to close the over-shift gap, at which point the view finally snapped to the new tail ("continues printing again new candles"). **Fix**: snapshot `m_idxs.size()` before the rebuild, compare to size after, shift by the delta — `filteredPrependCount = filteredAfter - filteredBefore`. The shift now matches the actual change in the filtered-array indexing so previously-visible candles stay anchored at their on-screen position regardless of how aggressively the session filter trims the merged bars. Stderr diagnostic prints when `filteredPrependCount != rawPrependCount` so any future regression is visible in logs. Build clean.

- [x] (unplanned, 2026-05-13) — **Clean cancel-all-subscriptions before IB Gateway disconnect.** User report: IB Gateway logs `[ERR] Can't write, socket client{1} is closing` after disconnecting / closing the app. Symptom is benign during the session (everything works normally — IB Gateway cleans up server-side state when the TCP socket drops) but the noise pollutes logs and can mask real errors. Root cause: both shutdown paths (`Disconnect()` menu click and the main-loop cleanup at app exit) called `g_IBClient->Disconnect()` (which stops the send thread + calls `eDisconnect()`) without first cancelling the per-instance subscriptions held by chart / trading / watchlist / WSH-calendar windows and the four `/ES` + `/NQ` futures market-health feeds. IB Gateway had queued outbound depth / market-data ticks for those still-active reqIds, tried to write them to our closing socket, and logged the failure. Account-level subscriptions (`ReqAccountUpdates(false)`, `CancelPnL`, `CancelPnLSingle`) were already explicitly cancelled, but per-window streams weren't. **Fix**: new `CancelAllSubscriptions()` helper in `main.cpp` (next to `DestroyTradingWindows`) walks every entry vector and queues cancels: `ChartEntry → CancelMarketData(mktId)` + `CancelHistoricalData(histId)` + `CancelHistoricalData(extId)` (only if `extStreamActive`); `TradingEntry → CancelMarketData(mktId)` + `CancelMktDepth(depthId, useL2())` + `CancelTickByTickData(tickId)`; loop 140–143 for the four futures market-health feeds; calls existing `WatchlistWindow::CancelAll()` on every entry and `WshCalendarWindow::CancelAll()`. Called from both `Disconnect()` (before the account-level cancels) and the app-exit cleanup block (before `g_IBClient->Disconnect()`). The send thread drains the queued cancels before `eDisconnect()` closes the socket, so IB Gateway sees orderly per-subscription cancels rather than a slammed TCP close with pending writes. Build clean.

- [x] (unplanned, 2026-05-13) — **TradingWindow DOM "Levels" dropdown ignored — only 25 rows render regardless of selection.** User report: "trading window I can't see the full levels — I can only see 25 levels on bid and 25 on ask but levels on the dropdown are assigned to 300." Root cause: `TradingWindow::setNumDepthRows()` (`TradingWindow.cpp:294`) was firing `OnDepthModeChanged(m_useL2)` to re-subscribe with the new row count. That callback is wired in `main.cpp` to cancel with `!useL2` (`main.cpp:1281`) — correct logic for the L1↔L2 toggle path (where `m_useL2` was just flipped, so `!useL2` gives the OLD mode's `isSmartDepth` flag). But `setNumDepthRows` doesn't flip the mode, so `!useL2` is the WRONG `isSmartDepth` flag — `cancelMktDepth(reqId, !useL2)` doesn't match the active subscription's flag, IB silently drops it, and the new `ReqMktDepth(numRows=300)` collides with the still-alive 25-row subscription on the same reqId. The new request is rejected and the original 25-row stream keeps flowing. **Fix**: split the callbacks. Added `std::function<void()> OnDepthRowsChanged` to `TradingWindow.h`; `setNumDepthRows()` now fires `OnDepthRowsChanged()` instead. The new lambda in `main.cpp` cancels with the **current** `useL2` (no flip) and re-subscribes with the new row count, so IB cleanly drops the old subscription and the user sees the full 300 levels. The toggle path (`OnDepthModeChanged`) is unchanged. Build clean; live IB smoke-test deferred (manual). 311/311 tests pass.

- [x] (unplanned, 2026-05-13) — **Futures chart candles disappear on live bar append** (follow-up to the 2026-05-12 pan-left fix, same root pathology in a different code path). User report: "futures chart window dissappear the data randomly in some moment and a new candle appears normally again — only happening with /ES and /NQ." The 2026-05-12 entry above fixed `PrependHistoricalData` but missed `UpdateLiveBar` (`ChartWindow.cpp:396`). `RebuildFlatArrays()` builds `m_idxs` as a **sequential filtered index** (`idx++` only on bars that pass the session filter) — for /ES futures with `useRTH=false` + `showOvernight=false` it drops the ~8-hour Overnight block per trading day, so the raw `m_series.bars.size()` is much larger than the filtered count. `UpdateLiveBar`'s new-bar branch was pushing `(n - 1)` to `m_idxs` where `n = m_series.bars.size()` (raw count), creating a huge gap in `m_idxs` (e.g. `[0, 1, ..., k-1, n-1]` with `n >> k`). The chart plots candles at those X coordinates, the auto-scroll `m_xMax = newIdx + 1` / `m_xMin = m_xMax - span` shifts the view to `[n-span, n]` — every previously-visible candle in `[k-span..k-1]` slides off-screen ("candles disappear randomly") and only the lone new bar at index `n-1` renders. The next full `RebuildFlatArrays` triggered by zoom, pan, or settings toggle renormalises the indices, candles snap back ("new candle appears normally again"). For stocks during RTH the filter drops ~0 bars and the bug is invisible. **Fix**: push `(double)m_idxs.size()` instead of `(n - 1)` — matches the sequential filtered indexing `RebuildFlatArrays` uses. `EnsureTodayBar` (line 459) already does the right thing (uses `m_xs.size()`, which equals the filtered count). Build clean; live IB smoke-test on /ES + /NQ during a session with several days of intraday data deferred (manual).

## Phase 17: Comprehensive State Persistence

Plan at `.claude/plans/state-persistence.md`. Goal: when the user shuts the app down and starts it again, every UI configuration (window layout, indicator toggles, auto-analysis settings, setup settings, L2/L1 mode, ladder rows, scanner filters, font size, etc.) comes back — not just content (watchlists, chart symbols, replay state) like the pre-Phase-17 era.

- [x] **Task #78** — `state-io.h` shared persistence helpers + tests (Task 1 of the plan, 2026-05-13) — `src/core/services/state-io.h` is a header-only library exposing `EnsureConfigDir()` (cross-platform `~/.config/ibkr-trading-app` resolution with `USERPROFILE` fallback on Windows), `ConfigFilePath(filename)`, `AtomicWriteText(path, contents)` (writes `<path>.tmp` then `std::filesystem::rename` over `<path>`; copy+remove fallback for cross-device-rename edge cases), `ReadTextFile(path, &exists)` (sets `*exists=false` for missing files — first launch isn't an error), `ParseStateBlocks(contents)` / `FormatStateBlocks(blocks)` (line-based parser/formatter for the established `INSTANCE:N + KEY:value` format used by `watchlists.cfg` / `chart-modes.cfg` / `replay-windows.cfg`), and typed accessors `GetBool/GetInt/GetDouble/GetString(block, key, dflt, [lo, hi])` with range clamping + NaN/Inf rejection. Comments (`#…`), blank lines, and malformed lines (no colon) are skipped; values containing colons round-trip (parser splits on first colon only — `BASE_URL:https://api.anthropic.com/v1` works). Keys sorted alphabetically per block in `FormatStateBlocks` for deterministic diffs. No IB API / ImGui dependency. `tests/test_state_io.cpp` adds 19 cases / 77 assertions under the `[state-io]` tag: parser edge cases (empty / comment-only / blank-line / single block with no INSTANCE / multiple INSTANCE blocks / comments + blanks interspersed / malformed-line skipping / colon-in-value preserved / whitespace trim / bad INSTANCE value → -1), `Format → Parse` round-trip with mixed-type fields, typed-accessor coverage (bool variants 1/0/true/false/yes/no, int parse/range, double NaN/Inf rejection, string defaults), filesystem helpers via a per-test `HomeOverride` RAII fixture that redirects `$HOME` to a temp dir and cleans up after. `tests-core` overall 311 cases / 1503 assertions; build clean.

- [x] **Task #83** — `scanner-settings.cfg` (Task 6 of the plan, 2026-05-13) — Per-ScannerWindow UI settings persistence covering asset class (Stocks/Indexes/ETFs/Futures), preset index into `kPresets[10]` (Top Gainers / Top Losers / Volume Leaders / Most Active / New 52-Wk Highs / New 52-Wk Lows / RSI Overbought / RSI Oversold / Near Earnings / Custom), filter bar expanded flag, all `ScanFilter` numeric ranges (price/%chg/volume/mktCap/RSI min+max), string filters (sector, exchange), filter-input char buffers (persisted alongside the parsed ranges so the input fields look identical across restart — accounts for the case where the user typed something valid that hasn't yet been applied to the range, or typed something invalid that the range parser ignored), 14 column-visibility toggles, sort column (clamped to the 16-value `ScanColumn` enum) + direction, and auto-refresh settings (toggle + 5–3600s interval clamp). `ScannerWindow.h` forward-declares `core::services::StateBlock` and adds public `SerializeSettings(StateBlock&) const` / `ApplySettings(const StateBlock&)`. All numeric fields clamped on apply: prices `[0, 1e9]`, %chg `[-1000, 1000]`, volumes `[0, 1e12]`, mkt cap `[0, 1e9]`, RSI `[0, 100]`, preset idx `[0, kNumPresets-1]`, sort col `[0, 15]`, refresh interval `[5, 3600]`. `ScannerWindow.cpp` includes `core/services/state-io.h` and implements both methods right after `getPresetLabel`. main.cpp gains `g_lastScannerSettingsHash` + `BuildScannerSettingsText` / `SaveScannerSettingsFile` / `LoadScannerSettingsFromFile` using the same hash-diff pattern as Tasks #81/#82. Per-second flush in `RenderTradingUI` (after trading-settings); sync flush in `DestroyTradingWindows`; load in `FinishConnect(false)` immediately after `LoadTradingSettingsFromFile`. No scan side effects on apply — `RunScan` is user-initiated; the user clicking "Scan" picks up the restored asset-class + preset + filter ranges. Build clean; 311/311 tests pass.

- [x] **Task #84** — `singleton-settings.cfg` (Task 8 of the plan, 2026-05-14) — Per-singleton-window UI settings persistence covering three windows in a single file: **Portfolio** (sort column clamped to `PositionColumn` 0–12, sort ascending, 7 column-visibility toggles for Description/AvgCost/CostBasis/RealP&L/DayP&L/DayChg/Weight, trade-history filter symbol buffer; `SortPositions()` called on apply so the restored sort takes effect immediately), **Orders** (history-tab filter symbol buffer, filter side 0–2, filter date buffer), **WshCalendar** (filter symbol, date-from/to strings, filter type 0–4, filter importance 0–3, sort column 0–4, sort ascending). Active-tab state on Portfolio and Orders is deliberately NOT included — both use ImGui `TabItem` which ImGui's `imgui.ini` already persists via the stable `io.IniFilename` path wired in Task #79 (confirmed during Task 7 investigation). Calendar-picker nav state on WshCalendar also excluded (ephemeral; closes between sessions). `state-io.h` extended: `StateBlock` gains `std::string windowName` field; `ParseStateBlocks` treats `WINDOW:<name>` lines as block delimiters (flushes the current block, sets `windowName`) alongside the existing `INSTANCE:<n>` delimiter; `FormatStateBlocks` emits `WINDOW:<name>` for blocks with non-empty `windowName` (takes precedence over `INSTANCE:<n>`). `tests/test_state_io.cpp` extended with 3 new cases / 22 assertions: `WINDOW:name` single-block parsing, multiple `WINDOW` blocks, and `INSTANCE` + `WINDOW` intermixed; the round-trip test now includes a `WINDOW:portfolio` block alongside `INSTANCE:0`/`INSTANCE:1` blocks. All three windows (`PortfolioWindow`, `OrdersWindow`, `WshCalendarWindow`) forward-declare `core::services::StateBlock`, add public `SerializeSettings`/`ApplySettings` methods, and include `core/services/state-io.h` in their .cpp files. main.cpp gains `g_lastSingletonSettingsHash` + `BuildSingletonSettingsText` / `SaveSingletonSettingsFile` / `LoadSingletonSettingsFromFile` using the same hash-diff pattern. Per-second flush in `RenderTradingUI` (after scanner-settings); sync flush in `DestroyTradingWindows` (after scanner-settings); load in `FinishConnect(false)` immediately after `LoadScannerSettingsFromFile`. Build clean; 314/314 tests pass (1525 assertions).

- [x] **Task #85** — Dirty-flag flush wiring (Task 9 of the plan, 2026-05-14) — Verified all save/load hooks across the seven `.cfg` files + `imgui.ini`. Each `Save*File()` uses hash-diff gating (`std::hash<std::string>{}(text)` vs last-written hash) so the per-second `RenderTradingUI` flushes are no-ops when nothing changed — no per-window dirty-flag plumbing needed across the 40+ toolbar/popup mutation sites. Per-second flushes: chart-settings, trading-settings, scanner-settings, singleton-settings (hash-diff'd) + chart-modes, watchlists, replay-windows (dirty-gated). Sync flush in `DestroyTradingWindows`: all seven files written before entries are cleared. Immediate save: app-prefs on every Settings UI change (font, default style, TWS sync toggle). Load order in `FinishConnect(false)`: watchlists → chart-modes → chart-settings → trading-settings → scanner-settings → singleton-settings → replay-windows → account data fan-out. `imgui.ini` via `io.IniFilename` set once in `main()` (Task #79) — ImGui handles its own flushing. Build clean, 314/314 tests pass.

- [x] (unplanned, 2026-09-01) — **News + WSH Calendar closed-state now persists across restart** — User report (live Linux + Windows): "I close News and WSH Calendar windows but they reload every time I restart the app." Root cause: window open/closed state (`m_open`) was never persisted for these two — both default `m_open = true` and are recreated fresh on every connect (`CreateTradingWindows()` unconditionally does `SpawnNewsWindow(0)` + `new WshCalendarWindow`). Task #86's closed-window guard only covered chart/trading/scanner/replay (whose per-instance `.cfg` save builders skip `!win->open()` entries so a closed instance doesn't respawn); News and WSH had no visibility persistence at all. Fix — **WSH** (true singleton): added `WSH_OPEN` to `WshCalendarWindow::SerializeSettings`/`ApplySettings`, so it rides the existing `singleton-settings.cfg` WSH block (per-second hash-diff flush + `DestroyTradingWindows` sync flush + `LoadSingletonSettingsFromFile` in `FinishConnect`, all already wired). **News** (multi-instance, but only instance 0 is ever recreated on restart — there is no news restore loop): added a `NEWS_OPEN` field to `app-prefs.cfg`. `main.cpp` gains `g_newsOpenPref` (staged from disk in `LoadAppPrefsFromFile`, applied to `g_newsEntries[0].win->open()` in `FinishConnect` right after `LoadSingletonSettingsFromFile`). `SaveAppPrefsFile` writes the live window's `open()` when it exists, else the staged value; `DestroyTradingWindows` snapshots `g_newsOpenPref` from the live window before deleting it so an exit-after-disconnect still records the close. Missing keys default to open, so upgrading users keep both windows on first launch and only stay-closed after they explicitly close them. Build clean.

- [x] (unplanned, 2026-09-01) — **Blank new chart windows now persist across restart** — User report: opening "+ New Chart" then exiting/restarting lost the chart, while a new Order Book came back (correctly re-assigned G+1). Root cause: chart instances are respawned on restore only by the `chart-modes.cfg` restore, which is **symbol-gated** (`SaveChartModesFile` skips charts with an empty symbol) — so a freshly-opened, still-blank chart at the highest index was never recreated. `LoadTradingSettingsFromFile` / `LoadScannerSettingsFromFile` have a spawn pre-pass (compute `maxInst` from saved blocks, `SpawnXWindow` up to it) but `LoadChartSettingsFromFile` did not — it only applied blocks to already-spawned charts. Fix: added the same spawn pre-pass to `LoadChartSettingsFromFile`; `chart-settings.cfg` has a block for every open chart (not symbol-gated), so the blank chart is now respawned and its settings/group applied. No double-spawn — the `while (maxInst >= size)` loop is a no-op for indices chart-modes already filled. Build clean.

- [x] (unplanned, 2026-09-01) — **News group + Notifications visibility now persist** — News window had no group persistence (it's multi-instance with no per-instance cfg; only instance 0 is recreated on restart) and the Notifications history window (a true singleton created in `main()`, default `m_open=false`) never saved its open state, so opening it and restarting lost it. Both added to `app-prefs.cfg`: `NEWS_GROUP` (staged in `g_newsGroupPref`, applied to `g_newsEntries[0]` in `FinishConnect` beside `NEWS_OPEN`, and snapshotted in `DestroyTradingWindows`) and `NOTIF_OPEN` (staged in `g_notifOpenPref`, applied to `g_NotificationsWindow->open()` right after it's `new`'d in `main()`). `SaveAppPrefsFile` reads the live windows when present, else the staged values; it already fires at app-exit before either window is destroyed. Build clean.

- [x] (unplanned, 2026-09-01) — **Flattened the Windows menu** — removed the intermediate `Windows → IBKR → …` submenu so the window list (Chart / Order Book / Scanner / News / Watchlist / Replay / Orders / Portfolio / WSH Calendar / Notifications + the "+ New" spawners) sits directly under `Windows`. The `ImGuiItemFlags_AutoClosePopups=false` push/pop now wraps the whole `Windows` menu body (previously scoped to the inner IBKR submenu). Build clean.

- [x] (unplanned, 2026-09-01) — **Portfolio window is now a group symbol source** — User request: clicking a position symbol should load that symbol into the chart / order book (DOM) / replay in the same group. `PortfolioWindow` gains a symbol-sync group like the other windows: `m_groupId` (default G1), `setGroupId`/`groupId`, and a `std::function<void(const std::string&)> OnBroadcastSymbol` callback. A `core::DrawGroupPicker(m_groupId, "##port_grp")` renders at the head of the positions-table toolbar (with a tooltip); clicking a position's Symbol `Selectable` fires `OnBroadcastSymbol(symbol)` in addition to setting `m_selectedPos`. `main.cpp` wires the callback in `CreateTradingWindows` to `BroadcastGroupSymbol(g_PortfolioWindow->groupId(), sym)` — which already fans out to chart (`SetSymbol`), trading (`ApplyTradingSymbol` → re-subscribes mkt data + depth), news, and replay (`SetSymbol`) windows in the group, so no new broadcast plumbing was needed. Group id persists via `PORT_GROUP` in the Portfolio block of `singleton-settings.cfg` (clamped `[1, kNumGroups]`). Build clean.

- [x] (unplanned, 2026-09-01) — **Chart / DOM / Replay / Scanner group selection now persists across restart** (follow-up: Portfolio + Watchlist already did, these three reset to G1). Root cause: group was never serialized for these windows, and the two dirty-flag-gated config files (`chart-modes.cfg`, `replay-windows.cfg`) don't flush on a *bare* group change (no symbol/style/load edit sets their dirty flag). Fixes: **Chart** — `GROUP` added to `ChartWindow::SerializeSettings`/`ApplySettings` so it rides the **hash-diff** `chart-settings.cfg` (auto-writes within 1 s of any serialized change; chart-modes was deliberately not used for exactly the dirty-flag reason). **DOM** — `TW_GROUP` added to `TradingWindow::SerializeSettings`/`ApplySettings` (hash-diff `trading-settings.cfg`). **Replay** — `replay-windows.cfg` already saves `GROUP:` and restores it via `setGroupId`, but only flushes on its dirty flag; added a per-frame group-change detector in `RenderTradingUI` (`ReplayEntry::lastGroupId`, seeded to −1 so no spurious connect-time save) that sets `g_replayWindowsDirty` when a replay window's group changes. **Scanner** — `SCAN_GROUP` added to `ScannerWindow::SerializeSettings`/`ApplySettings` (hash-diff `scanner-settings.cfg`). All clamp to `[1, kNumGroups]` on load. Build clean.

- [x] (unplanned, 2026-09-01) — **Skip data requests for unused features** (follow-up to the News/WSH persistence fix). Two gates so a non-entitled or feature-unused session stops issuing dead requests (which otherwise spam IB error 10276 "News feed is not allowed" for WSH and burn request-pacing budget): **(1) WSH Calendar deferred subscription** — `WshCalendarWindow::SubscribeConId` now queues `{conId, symbol}` in `m_pendingSubs` and issues no `reqWshEventData` while `m_open == false`; `FlushPendingSubs()` drains the queue on the closed→open transition detected at the top of `Render()` (`m_open && !m_wasOpen`). A WSH window that's never opened (or restored closed via `WSH_OPEN:0`) issues zero WSH requests; opening it backfills everything seen while hidden. Existing subscriptions are not cancelled on re-close (v1 — dominant win is the never-opened case). **(2) Scanner fundamentals column gate** — `ScannerWindow::wantsFundamentals()` returns `m_showMktCap || m_showPE`; `main.cpp`'s scan-completion handler skips the per-row generic-tick-258 (Reuters fundamental ratios) requests when both columns are hidden, in addition to the existing `g_scannerFundDisabled` (post-10358) gate. **(3) Benign-code log filter** — `main.cpp`'s `onError` no longer prints codes IB sends purely to acknowledge a cancel or an already-torn-down subscription (162 scanner-cancelled, 300 no EId, 310 no depth sub, 365 no scanner sub, 366 no hist query); the handling logic below the print is unchanged, only the stderr line is suppressed so the log stays signal-only. Version bumped to 1.1.15. Build clean.

- [x] **Task #86** — Multi-instance restoration + closed-window fix (2026-05-14) — Two coupled bugs fixed: **(1) Closed windows reappeared on next session** because every save builder (`SaveChartModesFile`, `BuildChartSettingsText`, `BuildTradingSettingsText`, `BuildScannerSettingsText`, replay save) iterated entries with `if (!win) continue` — closed windows (`m_open=false`) were still in the vector and got saved. Added `|| !win->open()` to every save-builder guard so closed windows don't reach disk. **(2) G2/G3 settings not restored because instances weren't spawned.** `CreateTradingWindows()` spawns only instance 0; the bounds check `b.instance >= g_XXXEntries.size()` in all four loaders silently skipped blocks for higher instances. Added pre-pass spawn loops: chart-modes restore computes `maxIdx` from saved blocks and calls `SpawnChartWindow` in a `while` loop to fill the vector; `LoadTradingSettingsFromFile` and `LoadScannerSettingsFromFile` do the same with their respective spawn functions. `restoredCharts` declaration hoisted out of the inner scope (before the spawn block) so the AAPL-seed gate below still sees it. Build clean, 314/314 tests pass.

- [x] **Task #82** — `trading-settings.cfg` (Task 5 of the plan, 2026-05-13) — Per-TradingWindow UI settings persistence: L2 mode toggle, exchange filter (saved by **name** so it survives the dynamic per-symbol smart-component list refresh — saving by index would point at the wrong exchange after the list rebuilds), depth row count, ladder-rows combo index, top-height + book-width splitter ratios, click-to-trade toggle, expand-spread toggle, and order-entry defaults (qty char buffer, side, order type clamped to all 13 `OrderType` variants, TIF, outside-RTH). `TradingWindow.h` forward-declares `core::services::StateBlock` and adds public `SerializeSettings(StateBlock&) const` / `ApplySettings(const StateBlock&)`. **Crucial**: `ApplySettings` sets fields directly (no setter calls like `setNumDepthRows`) so loading before any depth subscription exists doesn't fire `OnDepthRowsChanged` / `OnDepthModeChanged` spuriously — the spurious-cancel race we hit in Task #82-precursor (the depth-rows-fix from earlier this session). All numeric fields clamped on apply: `NUM_DEPTH_ROWS` to `[5, kDepthLevels=300]`, `LADDER_ROWS_IDX` to `[0, 13]`, splitter ratios to `[0.15, 0.85]`, `DEFAULT_TYPE` to `[0, 12]` (13 OrderType variants), `DEFAULT_TIF` to `[0, 6]`. Saved exchange filter name → looked up in current `m_exchangeList`; falls back to index 0 ("All") when name absent (initial load before smart-component fan-out arrives). `LADDER_ROWS_IDX` apply also re-derives `m_ladderRows` from the static `kLadderOptions[]` table to keep the two in sync. `TradingWindow.cpp` includes `core/services/state-io.h` and implements both methods between `setNumDepthRows` and `Render()`. main.cpp gains `g_lastTradingSettingsHash` + `BuildTradingSettingsText` / `SaveTradingSettingsFile` / `LoadTradingSettingsFromFile` using the same hash-diff pattern as Task #81. Per-second flush in `RenderTradingUI` (after the chart-settings flush); sync flush in `DestroyTradingWindows`; load in `FinishConnect(false)` immediately after `LoadChartSettingsFromFile`. No subscription side effects because `TradingWindow` doesn't auto-subscribe on connect — the user-typed-symbol path (`ApplyTradingSymbol`) later reads `numDepthRows()` / `useL2()` and issues the depth subscription with the restored values. Build clean; 311/311 tests pass.

- [x] **Task #81** — `chart-settings.cfg` (Task 4 of the plan, 2026-05-13) — Per-chart UI settings persistence covering every field on `IndicatorSettings` (10 toggles + 7 numeric params: SMA1/2 periods, EMA period, BB period+sigma, RSI period, VP bins), `AutoAnalysisSettings` (9 toggles + 7 params: swingK, trendLookback, donchianLen, maxLevels, minTouches, scanCap, trendChannel), `SetupSettings` (1 master + 6 numeric + 7 confluence + 2 advanced), and the splitter-level fields `m_useRTH` / `m_showOvernight` / `m_showLegend` / `m_volumeHeightRatio` / `m_rsiHeightRatio`. `ChartWindow.h` forward-declares `core::services::StateBlock` (avoids pulling `state-io.h` into every TU that includes `ChartWindow.h`) and adds two public methods `SerializeSettings(StateBlock&) const` / `ApplySettings(const StateBlock&)`. Both pure: no IB calls, no rendering side effects. All numeric fields are clamped on apply via `GetInt`/`GetDouble` bounds so a corrupted file can't break invariants (e.g. `SMA1_PERIOD` clamped to `[2, 500]`, `VP_BINS` to `[10, 200]`, `RR_MIN` to `[1.0, 10.0]`). `ChartWindow.cpp` includes `core/services/state-io.h` and implements both methods at the top of the file (just below `setInstanceId`, before `setTradingStyle`). main.cpp gains `g_lastChartSettingsHash` plus `BuildChartSettingsText()` / `SaveChartSettingsFile()` / `LoadChartSettingsFromFile()`. **Hash-diff persistence**: `SaveChartSettingsFile` formats all chart blocks, computes `std::hash<std::string>{}(text)`, and skips the write entirely when the hash matches the last-written value — so the per-second flush in `RenderTradingUI` is a no-op when nothing changed, eliminating the need for dirty-flag plumbing across the 40+ toolbar/popup mutation sites in `ChartWindow.cpp`. Sync flush in `DestroyTradingWindows` (after the existing chart-modes flush). Load order in `FinishConnect(false)`: `CreateTradingWindows()` → chart-modes restore block (sets style+symbol, fires `ReqChartData` with preset history horizon) → `LoadChartSettingsFromFile()` (layers user customisations on top of preset defaults that `setTradingStyle` just stamped). `m_useRTH` loaded *after* the chart-modes `ReqChartData` issued — the (rare) mismatch where the user's saved useRTH differs from `!isIntraday` just means a few extra bars get filtered at render time, not a correctness bug. `LoadChartSettingsFromFile` also stashes the on-disk hash so the immediately-following per-second flush is a no-op until the user changes something. Build clean; 311/311 tests pass.

- [x] **Task #80** — `app-prefs.cfg` (Task 3 of the plan, 2026-05-13) — Singleton-block persistence file for app-wide UI preferences. Three fields: `FONT_SIZE` (0=Small / 1=Medium / 2=Large), `DEFAULT_TRADING_STYLE` (int enum value 0=Scalping..4=Free), `SYNC_TWS_DISPLAY_GROUPS` (bool). `main.cpp` gains `SaveAppPrefsFile()` / `LoadAppPrefsFromFile()` / `ApplyAppPrefsToStyle()` and a new `g_defaultTradingStyle` global (default `Swing`). Load fires in `main()` immediately after `g_baseStyle = ImGui::GetStyle()` snapshot — before `ImGui_ImplGlfw_InitForVulkan` and the main loop, so the login window renders at the user's preferred font size on first frame. `ApplyAppPrefsToStyle()` sets `io.FontGlobalScale = kFontScales[fs]`, copies the baseline style, calls `ScaleAllSizes(kFontScales[fs])` — the same three-line sequence the existing Settings font-radio handler used inline (now factored out and shared). `g_twsGroupSync` is staged on load; the actual `SubscribeToGroupEvents` fan-out is left to the existing post-connect block in `FinishConnect` (line ~2238) which already inspects the global. Save fires immediately on every Settings UI change (font radio click, default-style combo, TWS toggle) — no per-second debounce since the writes are tiny and the Settings UI is rarely touched. Settings panel gains a "Default Trading Style (new charts)" combo populated from `TradingStyleLabel(Scalping..Free)`; selection writes the new global + saves. `SpawnChartWindow` calls `e.win->setTradingStyle(g_defaultTradingStyle, /*silent=*/true)` after construction when the default differs from Swing — silent so it doesn't enqueue an IB request, and `chart-modes.cfg` restore later overrides this for known charts. Construction defaults are unchanged so first launch (no `app-prefs.cfg`) keeps the existing behaviour (Medium font, Swing default, TWS sync off). 311/311 tests pass; build clean.

- [x] **Task #79** — ImGui `IniFilename` wired to config dir (Task 2 of the plan, 2026-05-13) — `main.cpp` sets `io.IniFilename = core::services::ConfigFilePath("imgui.ini").c_str()` immediately after `ImGui::CreateContext()` / `ImPlot::CreateContext()` and after the existing `ConfigFlags` setup. Pointer must outlive the ImGui context — backed by `static const std::string g_imguiIniPath` declared at the same scope. Before the fix, ImGui defaulted to `imgui.ini` in the binary's CWD, so window positions / sizes / dock arrangement / splitter regions / table column widths landed in different files depending on whether the user launched from a terminal vs the desktop vs an IDE — visible as "the workspace looks different every time I open the app." After the fix the file lives at a stable absolute path (`~/.config/ibkr-trading-app/imgui.ini`) and the layout is consistent across launches. `EnsureConfigDir()` returning empty (no `$HOME` / mkdir failed — extremely rare) is handled by leaving `io.IniFilename` at its default; ImGui still works, just without persistence. `#include "core/services/state-io.h"` added to main.cpp. Build clean.

- [x] (unplanned, 2026-05-09) — **Docking drop-target alignment fix on multi-viewport drag-and-dock** — Dragging a window already-floating in its own OS-level viewport back into the main app's dockspace produced misaligned blue drop-target highlights: the highlight tracked the wrong viewport, slid as the dragged window moved, and the window snapped to a dock node other than the one under the cursor. Root cause: ImGui's GLFW backend (`imgui_impl_glfw.cpp:743`) sets `ImGuiBackendFlags_HasMouseHoveredViewport` and reports the dragged viewport itself as the cursor's hovered viewport — when ImGui flags a dragged-for-docking window with `ImGuiViewportFlags_NoInputs`, the backend is supposed to make OS hit-test pass through it, but on Linux GLFW < 3.4 there's no `GLFW_MOUSE_PASSTHROUGH` (`imgui_impl_glfw.cpp:204-207`, `GLFW_HAS_MOUSE_PASSTHROUGH=0`) and on Win32 the WndProc hook is incomplete; on every OS the backend can end up reporting the dragged viewport as hovered, so `mouse_viewport_id = viewport->ID` (`imgui_impl_glfw.cpp:980-981`) ends up pointing at the dragged window instead of the dock target underneath. ImGui then trusts that report (`imgui.cpp:2013`) and routes drop-target detection to the wrong viewport — the dragged window itself — making the drop highlight visibly drift. **Fix**: clear `io.BackendFlags &= ~ImGuiBackendFlags_HasMouseHoveredViewport` immediately after `ImGui_ImplGlfw_InitForVulkan(...)` in `main.cpp`. With the flag cleared, ImGui ignores the backend value and falls back to `FindHoveredViewportFromPlatformWindowStack()` (`imgui.cpp:16654-16663`), which iterates viewports in z-order and explicitly **skips `ImGuiViewportFlags_NoInputs` viewports** before returning the topmost match — exactly what's needed during a docking drag. Build clean; live multi-viewport docking smoke-test deferred (manual).

## Phase 18: Options Chain Window

Plan at `.claude/plans/options-chain.md`. Singleton window for an underlying's
expirations × strikes with single-leg order tickets. Scope: stocks/ETFs only,
visible-row streaming, verticals planned (Task F, not yet landed). Branch
`feature/options-chain`, versions 1.3.0–1.3.3.

- [x] **Task A** — `core::Order` carries a non-stock contract. `ContractSpec`
  gains strike/right/tradingClass; `MakeContractFromSpec` gains an OPT branch;
  `PlaceOrder` uses spec-or-stock fallback (empty secType = legacy stock path,
  byte-identical). Unblocks every option order. `ContractSpec` moved to its own
  header (pure move) so `OrderData.h` need not depend on `ScannerData.h`.
- [x] **Task B** — IB service surface: `ReqSecDefOptParams` + four EWrapper
  overrides (`securityDefinitionOptionalParameter`/`…End`,
  `tickOptionComputation`, `tickGeneric` — the last two did not previously
  exist), four `IBMessage` variants + dispatch. `TickType` spelled `::TickType`
  to avoid the `core::TickType` (ReplayData.h) shadow. 4 dispatch tests.
- [x] **Task C** — `OptionData.h` (POD) + `OptionChain.h` (pure logic):
  MergeChainDefinition, FindAtmIndex/ClassifyMoneyness/StrikeRangeAroundAtm,
  DiffSubscriptions, SpreadNetPrice/QuoteMid. Deviation from plan §4:
  OptionChainMeta holds flat expirations + strikes (IB delivers two independent
  sets, not the pairing).
- [x] **Task D1** — window shell matching the design sketch: G-pill/symbol/
  strikes/cols/auto toolbar, underlying strip, expiry tabs with DTE, mirrored
  Calls|Strike|Puts table with ITM shading + ATM highlight + spot/±σ rules,
  legend. Singleton lifecycle + `Options Chain G<n>` Windows-menu entry +
  `WINDOW:optionschain` persistence block.
- [x] **Task D2** — live quotes via visible-row streaming (see architecture.md).
- [x] **Task E** — single-leg order tickets: click bid/ask (ask buys, bid
  sells) → ticket with qty/limit/TIF + live stats strip (ComputeStrategyMetrics)
  → confirm popup → PlaceOrder. Transmit-Instantly off by default.
- [x] **Task F** — vertical spreads (1.3.19). Two-click on the chain builds a
  vertical: click a strike's bid/ask (leg 1), then a different strike of the
  same expiry + right with the opposite action (leg 2). `core::ContractSpec`
  gains `std::vector<ComboLegSpec> comboLegs`; `MakeContractFromSpec` builds a
  `secType="BAG"` contract with the two `ComboLeg`s; `PlaceOrder`'s empty-secType
  fallback keeps the stock path byte-identical. Leg conIds are resolved with a
  `reqContractDetails` round-trip (reqIds 21004/21005 → `onContractDetailsFull`
  → `OnLegConId`, matched by expiry/strike/right), and the Send button is gated
  until both land. The order buys the combo at a signed net limit (positive =
  debit, negative = credit — the `limitPrice > 0` gate is relaxed for spreads),
  each leg carrying its own BUY/SELL. Net debit/credit shown live from the leg
  mids; the confirm popup renders both legs. Per-leg fills list separately in
  the blotter (v1). Pure net-price math (`SpreadNetPrice`, credit case) already
  covered under `[options][spread]`. Same branch / PR as single-leg.
- [x] **Task G** — docs (this entry + architecture.md + testing.md).
- [x] (unplanned, 2026-09-05) — **Order-ticket visual pass + resizable chain
  columns (1.3.23)**, all in `OptionsChainWindow.{h,cpp}` — no pure-logic
  changes, so no new tests. (1) **Selection outline**: `DrawChainTable`'s
  `priceCell` draws a 2px `AddRect` around a clicked bid/ask cell — green
  (`IM_COL32(64,200,96)`) for a buy leg, red (`IM_COL32(224,72,72)`) for a
  sell leg. A `stagedLeg(strike, right, isAsk)` lambda matches the staged
  `m_ticketKey`/`m_leg2Key` (buy ⇒ ask cell, sell ⇒ bid cell); works for both
  legs of a vertical. (2) **Two-column ticket band**: `DrawOrderTicket` split
  into a left child (`##opt_ticket_legs_col`, fixed `em(486)`) holding the
  legs table — `Leg | Symbol | Action | Expiry | Strike | Side | Bid | Ask`,
  one row per leg — and a right child (`##opt_ticket_order_col`, width 0 =
  right-justified to the window edge) after an `em(20)` gutter, holding the
  order controls. A spread now grows sideways instead of pushing Send/Clear
  off the bottom. `kTicketBandHeight()` changed from `static` to a non-static
  `const` method returning 7 line-heights for a spread, 5 for a single leg.
  (3) **Clickable price anchors**: single-leg shows `bid (opp) | mid |
  ask (nat)` as `SmallButton`s (labels swap with buy/sell — the marketable
  side is `nat`), each click snaps the tick-rounded value into the Limit
  field. (4) **Synthetic spread quote**: for a vertical, a `Spread` row under
  the legs shows net-bid (`Σ buy·bid − sell·ask`, passive) and net-ask
  (`Σ buy·ask − sell·bid`, marketable), both clickable into the Net field,
  signed `+`debit/`−`credit; the `net mid` button below completes a
  `net bid | mid | ask` set matching the single-leg layout. (5) **Right-column
  order**: stats strip (EXT / Delta / Theta / Max Prof / Max Loss) moved
  above the Qty/Limit/TIF row; actions row reordered to `Review & Send |
  Clear | Transmit Instantly` with an `Indent(em(16))` leading gutter, an
  `em(8)` vertical gap above the buttons (mirrors ChartWindow's trade panel),
  and an `em(24)` gap before the Transmit checkbox. (6) **Resizable chain
  columns**: `ImGuiTableFlags_Resizable` added to the `##optchain` table so
  every calls/puts greek/price column (and the Strike divider) is drag-sizable;
  widths persist via ImGui's per-table settings in `imgui.ini`. Build clean;
  committed `32c65a0`, pushed to PR #1; live smoke-test deferred (market
  closed at commit time).

- [x] (unplanned, 2026-09-05) — **Chain overlay/marker fixes + expiry tabs
  (1.3.24)**, all in `OptionsChainWindow.{h,cpp}`. (1) **Overlay-rect bug**:
  `DrawChainTable` read `GetItemRectMin/Max` *before* `EndTable`, so it captured
  the last cell (a sliver) instead of the table — the spot/σ rule guard rejected
  every rule and no lines ever drew. Moved the reads after `EndTable`; the
  spot/±SD overlays now span the table. (2) **`sigma`→`SD`** relabel on the rule
  labels + legend. (3) **ITM badges** (tastytrade-style): amber `^ ITM` on the
  calls side (row above the AT-M line) and `v ITM` on the puts side (row below),
  anchored to the captured spot-crossing y and the strike **cell** x-bounds
  (cursor + `GetContentRegionAvail`, not the text rect, so the puts badge sits
  past the cell). (4) **Spot marker**: the full-width spot line is replaced by a
  red `<` chevron at the strike cell's right border (no label). (5) **Strike
  column centered** — cell values, the `STRIKE` group label, and the `price`
  sub-header (rendered as centered text since `TableHeader` forces left-align).
  (6) **Auto-fit columns**: every greek/price column set to width 0 under
  `SizingFixedFit` (content auto-fit, still resizable); `STRIKE` pinned
  `WidthFixed | NoResize` so the mirror axis never drifts. (7) **Expiry tabs**:
  `DrawExpiryTabs` rebuilt as GFIS-style two-line tabs (`Oct 16 '26` over
  `42 DTE`) with an azure underline on the selected one, hover brighten + hand
  cursor. Default wraps to rows; a `▼`/`▲` toggle at the far-right of the
  underlying strip (`m_expirySingleRow`, persisted as `OPT_EXP_1ROW`) collapses
  to a single **drag-scrollable** strip (`BeginChild` + `SetScrollX`; a >6px
  left-drag pans and suppresses tab selection) with slim, semi-transparent,
  vertically-centered `<`/`>` chevrons. Tab text uses the current window's draw
  list so scrolled-out tabs clip to the strip. (8) **Legend removed** (ITM + SD
  now shown by the badges/lines; reclaimed the reserved line). No pure-logic
  changes, so no new tests. Build clean; live smoke-test deferred (market
  closed at commit time).

- [x] (unplanned, 2026-09-05) — **Close-to-unsubscribe + remove the "Auto"
  toggle (1.3.25)**, `OptionsChainWindow.{h,cpp}`. Closing the window used to
  leave its ~60 option market-data subscriptions streaming (Render early-returns
  on `!m_open` and never cancelled) — a market-data-line leak. `Render` now calls
  `CancelAll()` on the open→closed transition (guarded by `!m_quotes.empty()`);
  reopening re-subscribes the visible rows since the chain stays loaded. With
  closing as the real stop, the "Auto ON/OFF" button was redundant (it was a
  workaround for the stream never stopping), so `m_autoRefresh`, its
  `SyncSubscriptions` gate, the toolbar button, and the `OPT_AUTO` persistence
  were removed. Streaming is now simply: Load Chain + window open = stream;
  close = stop. Build clean (one transient GCC ICE on the version-bump full
  rebuild, passed on retry); live smoke-test deferred.

Derived-metric corrections (each verified against the real definition after an
initial wrong implementation): **expected move** → tastytrade straddle
weighting, not annualised IV; **IVx** → Cboe VIX-style variance-swap integral,
not ATM IV; **strategy metrics** verified against a real SPX ticket. BP Effect /
POP / P50 deliberately out of scope (plan §10b).

Live-testing fixes (found only by running against a paper Gateway — none caught
by the test suite, since the failures were all in UI↔callback wiring):
- **1.3.0** — CI never built this branch (push/PR triggers omitted
  `feature/**` and `fix/stability-review`), so every downloaded build was
  stale; fixed the triggers. Version bump made the running build identifiable.
- **1.3.1** — closing a Watchlist left an undeletable ghost menu entry (my
  earlier OPEN:0 persistence resurrected it every restart); closing now
  destroys the instance (null the slot, not erase — index-capturing lambdas).
- **1.3.2** — chain never loaded: the Load Chain button was dropped in the
  sketch rewrite, and OnRequestUnderlying/OnUnderlyingPrice were never wired.
- **1.3.3** — quotes rejected (IB error 200): tradingClass omitted from the
  streaming subscription (union-flatten mismatch); rejected contracts
  blacklisted; errors surfaced on the status line. Duplicate table header IDs
  fixed.

